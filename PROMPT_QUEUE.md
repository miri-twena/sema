# Prompt queue

Ordered work queue for Claude Code. Protocol: when asked to "take the next task from the queue" (or similar), pick the FIRST item with status `pending`, set it to `in_progress` (edit this file), execute the referenced prompt file fully, and when everything is green (`pytest`, `npm run lint`, `npm run build`) set it to `done` with today's date. Do exactly ONE item per request unless explicitly told to continue. If an item fails partway, leave it `in_progress` and note the blocker under it.

| # | Prompt file | Scope note | Status |
|---|---|---|---|
| 22 | `demo_freshness_prompt.md` | Relative END_DATE in both data generators (always yesterday, --end-date override for tests), knowledge.yaml dates kept aligned, reseed both tenants — demo never ages | done (2026-08-01) |
| 27 | `table_pager_arrows_prompt.md` | Bug: table pager chevrons reversed inside RTL answers ("> Page 4 of 20 <") — fix direction in ALL pager instances, verify visual + click behavior, both languages | done (2026-08-01) |
| 23 | `popover_zorder_sweep_prompt.md` | Popover/kebab clipping bug CLASS fix: extract the sidebar portal-menu solution into one shared component, migrate every kebab/confirm popover app-wide (reported now: Users screen row menu), verify at list-bottom/mid-scroll/narrow | done (2026-08-01) |
| 24 | `users_pagination_prompt.md` | Users screen pagination: server-side page/page_size (25), reuse the Audit log pager pattern, search+filter compose with paging, pager hidden when total ≤ page size | done (2026-08-01) |
| 25 | `i18n_language_files_prompt.md` | UPDATED language decision: central locale files (en/he, typed, parity-tested) + full Hebrew chrome sweep (app shell + admin) when org opts into Hebrew. Login stays English; answers follow question; layout anchored. Supersedes the "2 strings only" scope of chat_placeholder_language_prompt | done (2026-08-01) — app shell + Users admin screen fully done; Data sources/Semantic model/Org settings/Audit log's own chrome NOT yet migrated, see evidence for scope note |
| 26 | `rerun_conversation_prompt.md` | "Rerun" in the conversation ⋯ menu: re-asks the source conversation's FIRST question in a brand-NEW conversation (fresh SQL/data) — NOT retry-in-place; source untouched; localized label. Best after #23 (shared portal menu) and #25 (locale files) | done (2026-08-01) |
| 18 | `backend_qa_prompt.md` | Backend QA sweep before the first customer: full authz route matrix (fails on unclassified new routes), tenant-isolation suite, LLM/DB failure resilience, concurrency + SQLite WAL, per-user daily LLM soft cap, and an honest `docs/qa_findings.md`. Best AFTER #17 (uses SEMA_ENV gating in the matrix) | done (2026-08-02) |
| 28 | `home_ask_bar_top_prompt.md` | Approved mockup (mode B): home screen ask input moves to the top under the greeting — prominent, cycling live placeholder from real suggested questions, bottom bar removed on home only, "/" focuses, conversation layout untouched | done (2026-08-02) |
| 29 | `home_hebrew_polish_prompt.md` | Hebrew home review fixes: attention line below the ask input (above brief), Hebrew headings right-aligned (text only, layout stays anchored), guidance sentence above the input, cycling placeholder in Hebrew when org=he. ALSO: new-conversation empty state's intro block (header + guidance + suggested cards) moves to the TOP of the pane; RTL punctuation fix (colon at wrong end); home = app anchor (always first screen on entry, SEMA logo/wordmark always navigates home) | done (2026-08-02) |
| 30 | `new_conversation_canvas_prompt.md` | NEW FEATURE (approved mode A): "New conversation" opens a clean centered ask canvas — headline, hero input (reuses home top-bar behaviors), 4 recommended-question cards, "Trending in your company" chips (respects toggle + data scopes), zero dashboard elements. Supersedes §5 of home_hebrew_polish. Run AFTER #29 | done (2026-08-02) |
| 11 | `auth_login_prompt.md` — Parts A+B | Real auth: email one-time code + sessions (A), Google/Microsoft SSO (B). Requires a real HTTPS host (secure cookies, OAuth callbacks). Unblock per checklist שלב 5 | **blocked — waiting for hosting** |

Status legend: `pending` = take per protocol · `in_progress`/`done` = per protocol · `blocked — …` = NEVER pick this up, even if it's the only item left; it becomes `pending` only when the human removes the block.

## Done (verified against the codebase, 2026-08-01)

| Prompt file | Evidence |
|---|---|
| `new_conversation_canvas_prompt.md` | New `NewConversationCanvas.tsx` -- a screen genuinely separate from the home dashboard (no brief/KPIs/attention line, confirmed absent live), reusing rather than duplicating: the SAME `ChatInput` component (`variant="hero"`, wrapped in its own `ring-2 ring-primary/15` for the mockup's "accent border + soft ring" look, since ChatInput itself stays unmodified), the SAME `useCyclingPlaceholder` hook + `limitPool` helper (reused verbatim for the "cap at 4 cards" rule instead of a second untested `.slice`), the SAME `QuestionChip` component for trending. New `t.newConversation` locale namespace (headline "What would you like to know?" / "מה תרצו לדעת?" -- neutral PLURAL Hebrew per AGENTS.md's gender-neutral-user-address rule, distinct from the SEMA-directed feminine imperatives used elsewhere; subtitle mirrors the home guidance line's phrasing minus "SEMA", matching the prompt's exact English text). **State machine (the real design work):** `App.tsx` gained a `homeView: "home" | "canvas"` state, orthogonal to `chat.turns` emptiness -- `showHome`/`showCanvas` are both `empty && !chat.loading`, gated further by `homeView`, so they're mutually exclusive and jointly exhaustive whenever the chat is empty (never both, never neither, matching the established `showHome`-only pattern from `home_ask_bar_top_prompt.md`). `newChat` split into two distinct actions: `goHome` (dashboard) and `startNewConversation` (canvas) -- wired to the sidebar's two SEPARATE existing props (`onGoHome`/`onNewConversation`, previously both pointing at the same function by coincidence, now genuinely different), the main header wordmark (`goHome`), and the admin panel's "Back to SEMA" (`goHome`). Every "conversation went away" fallback (archive/delete the active conversation, either list) now explicitly lands on `homeView: "home"` rather than leaving it at whatever it was last set to. The "/" shortcut and the bottom composer's hide condition both extended from `showHome` alone to `showHome || showCanvas`. Scope-filtering and the trending-toggle (item 4's "respects data scopes/toggle" ask) needed ZERO new frontend code -- confirmed by reading the backend: `/api/popular-questions` already applies `_question_is_blocked` scope filtering and the home-config trending toggle server-side, so the SAME `popularQuestions` prop App.tsx already fetches for Home arrives pre-filtered; the canvas just renders it, identically to Home's own trending section. **Tests:** no new unit tests needed beyond reusing `limitPool`'s existing coverage (this codebase has no component-render test infra, confirmed again -- the prompt's own "canvas renders all sections per config" / "card click submits" / "no empty conversation on abandon" criteria are exactly the class of behavior this project always covers via live verification instead, not synthetic component tests). `npm run lint`/`build`/`vitest` (143, unchanged) all green, locale parity test passing with the new namespace. `pytest` 931 passed (zero backend changes -- this is a 100% frontend feature; scope/trending filtering already existed and needed no touching). **Verified live end-to-end against the real dev stack in BOTH languages:** Hebrew -- "New conversation" opens the canvas (headline/subtitle/hero input/trending, dashboard elements confirmed absent from the page text), autofocus confirmed (`document.activeElement` was the textarea on open), "Go to home" (a DIFFERENT sidebar button) correctly returns to the full dashboard instead, not the canvas. Queried `/api/conversations` before and after: opening/abandoning the canvas repeatedly left the count at 89 (unchanged, confirming item 7's "abandoning creates NOTHING"); clicking a recommended card submitted that exact question, transitioned to the normal streaming/progress conversation UI, and the count became 90 with the new conversation's real title once the answer completed (confirming both "card click submits" and "first submission transitions to normal conversation UI") -- deleted the test conversation afterward, confirmed back to 89. English chrome (flipped via the real admin API, reverted after) -- confirmed the identical canvas renders correctly with the English headline/subtitle. |
| `home_hebrew_polish_prompt.md` | **Ambiguity resolved with the user first (item 5's exact placement wasn't fully determined by the prompt text alone):** asked where the "Start a conversation" block should land relative to the ask input and the rest of the top-of-page cluster; user chose "immediately after the ask input field." Final order, confirmed live in both languages: greeting -> guidance line -> ask input -> Start a conversation (suggested cards) -> attention line ("הנה מה שדורש...") -> brief chips -> daily brief -> business overview -> top recommendation -> trending (kept separate, at the bottom, since the prompt only named the suggested-cards block as moving). **1+3 (reorder + guidance line):** `HomeDashboard.tsx` restructured to this order; new `t.home.askGuidance` locale key ("Ask SEMA anything about your business — in plain language." / "אפשר לשאול את SEMA כל שאלה על העסק, בשפה חופשית." -- the EXACT neutral Hebrew phrasing the prompt specified, not the rejected feminine-imperative "שאלי" alternative) sits between the greeting and the input. **5 (discovery block split):** the old single "discovery" section (curated starters + trending, one guarded block) split into two independent sections -- starters moved up next to the input (keeping the `home-preview-questions` admin-preview-scroll-anchor id, since that's the block the admin's "questions" config panel actually edits), trending stays at its old spot as its own section. **2+6 (Hebrew heading alignment + RTL punctuation):** every static UI heading (`SectionLabel`, used by all section titles; the greeting; the new guidance line; the moved attention line; `DailyBrief.tsx`'s own two inline heading spans) now gets `dir="rtl"`/`dir="ltr"` from `useUiLang()` -- right-alignment falls out for free from the browser's own direction-relative `text-align: start` default (confirmed empirically: `textAlign: "start"`, `computedDir: "rtl"` for the Hebrew guidance line), so no extra className or mirrored flex-direction was needed anywhere (AGENTS.md's "layout is anchored" rule intact -- verified no page/card/grid physically reordered). **4 (Hebrew cycling placeholder, a real backend gap, not just a frontend bug):** root-caused -- `suggested_questions` had NO language concept anywhere (one flat, English-only list per client in `config/clients.yaml`, or an admin's free-text home_config override); the frontend cycling logic itself (built in `home_ask_bar_top_prompt.md`) was already correct. Fixed with a new `suggested_questions_he` YAML key per client (real Hebrew translations added for both ecommerce and insurance, reusing phrasing already established elsewhere in the app, e.g. "למה ההכנסות ירדו במרץ 2026?") and a new `_client_static_suggested_questions(client_cfg, lang)` helper in `api/main.py` that picks the Hebrew variant when `org_settings.default_language == "he"` -- wired into both `_client_model` (`/api/clients`) and the admin home-config preview endpoint. An admin's own published override (whatever language they typed) still always wins over BOTH language variants, unchanged and explicitly tested. 6 new backend tests (`tests/test_suggested_questions_language.py`): pure-function language selection, empty-Hebrew-list treated as unconfigured, the REAL `config/clients.yaml` content end-to-end via `/api/clients` in both languages (catches a future accidental deletion of the Hebrew variant, not just a synthetic fixture), and the override-always-wins guarantee. **8 (home is the app's anchor):** (a) `App.tsx`'s main `useChat()` call dropped its `persistKey` (`"sema:chat"` -> `null`) -- a fresh load/refresh no longer restores a localStorage-cached transcript at all (confirmed the OLD mechanism existed and would have auto-reopened the last conversation; a `reset()`-on-mount alternative was rejected because the hook's initial state reads localStorage synchronously on first render, which would have flashed the old transcript for one paint before clearing it). (b) TWO wordmark entry points now navigate home: the sidebar's brand button already did (pre-existing, confirmed unchanged); the MAIN PANE's own "SEMA" header (visible even on mobile where the sidebar is tucked into a drawer) is now also a real `<button aria-label="Home">` wired to the same `newChat` action. The admin panel's "Back to SEMA" now also resets chat state to home instead of merely closing the panel back to whatever was underneath, per the prompt's explicit "aligns with this" instruction. Verified live, not assumed: opened a real conversation, reloaded the whole page, landed on home (not the conversation) with `localStorage["sema:chat"]` no longer read; opened a conversation again and clicked the header "Home" button, confirmed it returned to the full home dashboard (greeting + guidance line both present). **Tests:** `npm run lint`/`build`/`vitest` (143, unchanged -- no new frontend unit-testable logic this task beyond what's already covered structurally) all green, locale parity test (`locales.test.ts`) confirmed passing with the new `askGuidance` key. `pytest`: 931 passed (925 prior + 6 new), zero failures this run (the intermittent Windows clock-resolution flake in `test_org_settings.py`, root-caused in the prior queue item, did not reproduce). **Verified live end-to-end against the real dev stack in BOTH languages:** Hebrew chrome -- full page-text dump confirmed the exact required order and exact guidance-line wording; English chrome (flipped via the real admin API, reverted after) -- confirmed the identical order renders correctly left-to-right with the English guidance line. No stray test data left behind (no new conversations were created during this verification pass, confirmed by not submitting through the ask input at any point). **Follow-up, same day (user flagged via a live screenshot that text-only alignment wasn't enough):** the executive-brief alert-chip row and `DailyBrief.tsx`'s own icon+label header row stayed visually LEFT-anchored in Hebrew -- `dir` had only been applied to the text INSIDE them, not the `flex` row itself, and `justify-content`/child order for a `flex-direction: row` container resolve against the CONTAINER's own direction, not its text's. Added `dir={dir}` to the row elements themselves (the alerts row, `DailyBrief`'s heading row, and proactively the same icon+label pattern in the Business Overview header and the Trending header, since leaving those inconsistent would just reproduce the identical complaint) -- confirmed live via `getBoundingClientRect()` that the chips and the "תקציר יומי" icon+label group now sit flush against the row's right edge. `npm run lint`/`build`/`vitest` (143, unchanged) all green; no backend touched. |
| `home_ask_bar_top_prompt.md` | **1 (placement):** `HomeDashboard.tsx` now renders the ask input as the SECOND thing on the page (right after the greeting/subtitle, before the brief chips), full content width, via `ChatInput`'s new `variant="hero"` (larger `text-base`/`py-2` sizing, a leading `Sparkles` icon, bigger `w-10 h-10` send/stop button) -- reused rather than duplicated, so behavior (auto-grow, Enter-to-send, loading/stop state) is byte-for-byte identical to the in-conversation composer, not a second implementation to keep in sync. The old bottom bar is now conditionally rendered in `App.tsx` on the EXACT SAME `showHome = empty && !chat.loading` flag that gates `HomeDashboard` itself, so it's structurally impossible to show both or neither. **2 (behavior):** submitting the hero input calls the SAME `sendQuestion`/`chat.send` path as every other ask entry point (discovery chips, bottom composer) -- confirmed live, not assumed: a real question asked from the hero input produced a real grounded answer (KPIs/chart/recommended actions), and immediately afterward the layout correctly flipped to the bottom-bar conversation view with the hero input gone. Autofocus (`ChatInput`'s new `autoFocus` prop) checks `matchMedia("(pointer: coarse)")` internally so it only fires on non-touch devices -- confirmed via the browser pane (`pointerCoarse: false` -> textarea was `document.activeElement` on mount). Found and fixed a real bug of my own making while wiring this up: the mount-time autoFocus itself was triggering the "stop cycling on focus" handler immediately, meaning the placeholder would never cycle even once on desktop -- fixed with a `suppressNextFocus` ref that distinguishes the programmatic autofocus from a REAL subsequent user focus (click/Tab), which still stops cycling correctly. **3 (cycling placeholder):** new `hooks/useCyclingPlaceholder.ts` cycles through up to 4 REAL suggested questions (the same `suggested` prop already sourced from the admin-customizable, home_config-effective `/api/clients` response -- confirmed live via direct API fetch that this org's list is exactly what the admin configured, org-customization included, per the task's own requirement) every 3s, stopping permanently on real focus/typing; respects `prefers-reduced-motion` by never starting the interval (reused the existing `prefersReducedMotion()` from `lib/chart.ts` rather than a second copy). Direction follows the CURRENT placeholder's own content via the existing `lib/rtl.ts dirOf()` (a new `placeholderDir` prop on `ChatInput` overrides the org-chrome-language default specifically for this case, since a suggested question can be in either language independent of the org's chrome setting) -- confirmed live (`dir="ltr"` for an English question). Core cycling logic (`limitPool`/`nextIndex`/`shouldCycle`) extracted as pure functions and unit-tested (this codebase has no component-render test infra, confirmed again, so logic was made testable the same way `loginReducer` already established the pattern) rather than only covered by live testing. **4 (hierarchy):** confirmed live in both languages -- order is greeting -> ask -> brief chips -> daily brief -> business overview -> top recommendation -> discovery/trending, unchanged from before except the ask input's new position. **5 ("/" shortcut):** new pure `lib/homeShortcut.ts shouldFocusAskInput()` (unit-tested: ignores modifiers, ignores while a modal/drawer/drill panel is open, ignores while focus is already in an input/textarea/select/contenteditable, focuses otherwise) wired into a `window` keydown listener in `App.tsx`, active only while `showHome`. Confirmed the underlying logic fires correctly via a directly-dispatched `KeyboardEvent` in the live browser (the Browser pane's own synthetic `computer key` action for "/" doesn't reliably reach `window` as a trusted event in this environment -- a tooling quirk, not a code bug, verified by dispatching the identical event shape programmatically and observing the textarea gain focus). **6 (locale):** audited for new user-facing strings -- found none: the cycling placeholder's content is either a real (already-existing, possibly org-customized) suggested question or falls back to the pre-existing `t.chat.placeholder`, and every aria-label reuses `ChatInput`'s existing `placeholder || defaultPlaceholder` pattern verbatim, so no locale file changes were needed. **Also fixed:** `HomeConfigScreen.tsx`'s read-only live preview (which renders the same `HomeDashboard` component) needed a no-op `onAsk={() => {}}` (mirroring its existing `onPick={() => {}}` no-op convention) and deliberately does NOT pass the new `autoFocusAsk` prop, so the admin's own form focus is never hijacked when the preview re-renders on every draft edit -- caught by `tsc -b`, not assumed. **Tests:** `hooks/useCyclingPlaceholder.test.ts` (10) + `lib/homeShortcut.test.ts` (8), all pure-logic per the established no-component-render-infra convention; no existing vitest needed adjusting (none exercised the home/App/ChatInput components directly, confirmed via search) -- covered by live verification instead. `npm run lint`/`build`/`vitest` (143, +17) all green; `pytest` (925 passed, the known Windows timestamp-race flake in `test_org_settings.py` did NOT reproduce this run) unaffected (zero backend files touched). **Verified live end-to-end against the real dev stack in BOTH languages:** Hebrew-chrome org (this session's default) -- hero input present with correct RTL-safe layout, one ask entry only, real submit-to-answer flow confirmed with a full grounded answer (KPIs, chart, recommended actions) rendered identically to the bottom composer's own output; flipped the org to English via the real admin API and confirmed the identical layout/order/hero-input renders correctly in English chrome too, then reverted back to Hebrew (same demo-hygiene pattern established in prior sessions). Test conversation created during live verification was deleted afterward (confirmed via direct API query that the tenant's conversation list matches its pre-test state exactly). |
| `backend_qa_prompt.md` | **1 (authz matrix):** `tests/test_authz_matrix.py` auto-discovers every registered FastAPI route (68 today) via `app.routes` and cross-checks each against a hand-maintained `ROUTE_CLASS` allowlist (`admin` vs `open`) -- deliberately NOT inferred from whether the code happens to call `Depends(require_client_admin)`, since that would silently "pass" a route that forgot the dependency, exactly the bug class this exists to catch. `test_every_registered_route_is_classified` fails the moment a new route has no entry. **Caught a real, pre-existing gap immediately:** `GET /api/admin/alerts/templates` and `GET /api/admin/alerts/metrics` had NO admin dependency at all despite living under `/api/admin/` -- fixed (added `Depends(require_client_admin)`, matching every sibling route; one pre-existing test in `test_api_alerts.py` that called them unauthenticated was updated + a regression test added). No `SEMA_ENV=production`-gated dev-only route exists anywhere (re-confirmed via the same route walk) -- nothing to test there yet, noted in the findings doc. **2 (tenant isolation):** `tests/test_tenant_isolation.py` (19 tests) proves nothing leaks ecommerce↔insurance across conversations/threads, audit, org settings, home config, org alerts, semantic drafts+versions, data-source connections/requests/uploads, and users -- direct-id cross-tenant fetches 404 (never 403, no existence leak). Found and documented (not silently papered over, per the task's own instruction) a real store-layer gap: `TurnFeedbackStore` performs ZERO `client_id` filtering on reads -- safe today only because `api/main.py` always validates conversation ownership via `conversation_store` first; both a store-level test proving the gap and a route-level test proving the guard holds. The mocked-identity limitation (non-admin conversation routes take `client_id` as a plain query param, not from identity) is documented explicitly. **3 (failure resilience):** `tests/test_failure_resilience.py` -- LLM timeout/429/500 failover was already covered by existing tests (`anthropic.APIError` is the base class all three subclass), so this file covers the gaps: a truncated/malformed model response never crashes the loop (degrades to an empty answer, flagged as a low-severity UX polish item, not fixed), a FAILED turn never corrupts a conversation (persists nothing; the next question still works, both for `/api/chat` and `/api/chat/stream`), `AgentTools.run_sql` gracefully turns a Postgres connection failure into a structured `{"error": ...}` result (first line only, no multi-line driver detail leak), `/healthz` recovers without a restart, and conversation state survives a simulated process restart (a fresh Store instance against the same SQLite file). Structural checks confirm both LLM (60s timeout / 2 retries) and SQL (5000ms `statement_timeout`) timeouts were already correctly configured. **4 (concurrency + WAL):** real bug found and fixed -- all 12 of the app's SQLite-backed metadata stores opened connections in sqlite3's default rollback-journal mode; new shared `sema_core/sqlite_utils.connect()` (enables `PRAGMA journal_mode=WAL`) now backs every one of them. `tests/test_concurrency.py`: 20 parallel `/api/chat` requests complete with zero cross-request bleed (p50=594ms/p95=852ms, mocked LLM+Postgres); 60 parallel writes spread across 3 stores sharing ONE file (the real production shape) complete in ~2.7s with zero "database is locked" errors. **Bigger finding surfaced by accident while building that same test with Postgres left live:** the SQLite layer isn't the pilot's real concurrency ceiling -- `SEMA_DB_POOL_MAX` (default 5 per client/role) is. 20 concurrent chat requests against real Postgres drove latency to 10-45s and threw `psycopg2.pool.PoolError: connection pool exhausted` repeatedly (silently swallowed by `_pulse_context`'s fail-open design, so no user-facing error, just severe invisible slowness) -- every single chat request, not just ones running SQL, spends a pooled connection building its `[SEMA-CONTEXT]` pulse block. Reported as the sweep's highest-severity finding rather than silently fixed (real work: raise the pool default and/or stop spending a connection on pulse-context for every turn). **5 (daily query cap, a new feature, not just tests):** `sema_core/query_limit_store.py` (SQLite, one row per client/org_user/UTC-day) + `SEMA_DAILY_QUERY_LIMIT` (default 200/user/day, 0 disables) wired into both chat routes via `_check_daily_query_limit(...) or get_response(...)` -- a blocked question flows through the EXACT SAME persistence/streaming code every other answer does, no special-casing. Over the cap returns `mode="access_denied"` (same restrained rendering as a data-scope refusal), bilingual per the QUESTION's language (reusing `prompts._question_script_hint`, per AGENTS.md's "answers follow the question, never the org setting" rule), and audits `usage.limit_reached` exactly ONCE per user per day (a `notified` flag), not once per over-the-cap request. `tests/test_query_limit.py` (14 tests): under/at/over the cap, persistence of the capped turn, limit=0 disables, reset on a new UTC day, both languages, both `/api/chat` and `/api/chat/stream`, and the "unscoped identity = uncapped" fallback. **`docs/qa_findings.md`:** the 2 fixed-and-verified bugs above, the new feature, and 5 reported-not-fixed findings each with severity + recommended fix + timing (the Postgres pool ceiling; the truncated-response empty-answer edge case; the pre-existing tenant-isolation/mocked-identity limitation; the `TurnFeedbackStore` filtering gap; and a handful of existing `/api/chat`-path tests that transitively depend on a live Postgres connection, root-caused but not touched to keep this sweep's diff scoped). Also root-caused (not just re-flagged) the long-standing `test_org_settings.py::test_update_flags_conflict_when_stale_but_still_applies` flake mentioned in nearly every prior queue entry: a genuine Windows `datetime.now()` clock-resolution collision (two calls in the same test can return the byte-identical ISO timestamp), confirmed directly and reproducible when run alongside its own sibling tests, unrelated to any code touched this session. `pytest`: 918 tests (735 pre-existing + 183 new across the 5 new files +1 modified), 917 passed + that one pre-existing flake (passed 5/5 in isolation, failed 3/3 as part of its own file/the full suite -- a genuine race, not a regression: confirmed via `git diff --stat` that this session never touched `test_org_settings.py` or `org_settings_store.py`'s update/timestamp logic). `npm run lint`/`build` both green (zero frontend changes this task). **Follow-up in the same session (user asked to fix the pool-exhaustion finding rather than leave it reported):** root-caused ONE level deeper than the original writeup -- `sema_core.cache.ttl_cache` only locked the cache CHECK, not the compute, so N concurrent callers racing a COLD cache key (every restart, every TTL expiry) each independently ran the underlying DB-hitting function; 20 concurrent chat requests could mean up to 20 simultaneous redundant Postgres queries for identical data, not 20 legitimately-different ones. Fixed with a single-flight `ttl_cache` (per-key lock: one caller computes, concurrent callers for the same key wait and reuse the result; different keys never block each other) -- `tests/test_cache.py` (6 new tests, incl. 20 threads racing a cold key producing exactly 1 real computation) + a new `tests/test_concurrency.py` test proving it on the actual implicated function (`queries.get_data_bounds`, mocked `run_query`, 20 concurrent callers -> 1 real call). `SEMA_DB_POOL_MAX` default also raised 5 -> 20 as real headroom on top of that fix (`sema_core/settings.py`, `.env.example`), not a band-aid over the still-there stampede. `docs/qa_findings.md` updated: the finding moved from "Not fixed -- reported" to "Fixed", with the deeper single-flight root cause documented. Full suite re-run: 924 passed + the same one pre-existing unrelated flake (confirmed again). |
| `rerun_conversation_prompt.md` | **Semantics, exactly as specified:** a brand-NEW conversation asking the source's FIRST user question fresh (current data/SQL/semantic model) -- not a same-conversation retry, not the whole turn sequence (MVP scope: only the first question, since later turns depend on earlier answers and would drift; noted inline in `ConversationActions.onRerun`'s own doc comment, not built as a V2 submenu per the task's explicit instruction not to). **Menu:** `ConversationItem.tsx` gained a `RefreshCw` "Rerun" item placed right after Rename in the active-conversation branch, and as the FIRST item in the archived branch (that branch has no Rename to place it after, but "archived conversations -- Rerun allowed" from the task's edge cases needed it there too) -- gated on a new `canRerun = message_count > 0` check so an empty/broken conversation hides the option entirely rather than offering a rerun with nothing to rerun. Label comes from the locale files exactly as specified (`menus.conversation.rerun`: "Rerun" / "הרצה מחדש"), continuing straight off `popover_zorder_sweep_prompt.md`'s portaled menu and `i18n_language_files_prompt.md`'s locale mechanism -- both prerequisites this task named. **Logic:** `lib/conversations.ts` gained `firstUserMessage(detail)`, a pure extraction of the transcript's first user turn (kept out of the component so it's unit-testable without a DOM, same rationale the file's own header comment already states for `turnsFromDetail`). `App.tsx`'s new `rerunConversation(id)` fetches the source's full detail (`api.conversation`), extracts the first question via that helper, then calls the EXISTING `chat.reset()` (clears the current view AND `conversationIdRef` -- a ref write, so it's synchronous, not deferred to a re-render) immediately followed by `chat.send(firstQuestion)` -- reusing the normal ask flow verbatim rather than adding a parallel code path, per the task's own "no backend changes expected" framing. Wired into `conversationActions.onRerun`; `archivedConversationActions` picks it up automatically via its existing `{...conversationActions}` spread, which is what makes "Rerun allowed from Archived" fall out for free instead of needing separate wiring. **No backend changes** (confirmed true, not just assumed -- the whole feature composes two already-existing endpoints/hooks). **Tests:** 2 new vitest cases for `firstUserMessage` (returns the FIRST question when there are several turns, not a later one; returns `undefined` for both a truly empty transcript and an orphaned-answer-with-no-user-message edge case) -- the rest of the feature (the menu item's visibility, the actual rerun-creates-a-new-conversation behavior) is integration-level behavior tightly coupled to `useChat`/`useConversations`, which this codebase has no component-render test infrastructure for (confirmed, same conclusion reached for `usePopoverMenu`/`useAdminUsers` earlier this session) -- covered by live verification instead, matching established convention. `npm run lint`/`build`/`test` (126, +2) all green; `pytest` 734 passed (zero backend changes, the previously-flaky `test_org_settings.py` timestamp-race test passed clean this run). **Verified live end-to-end against the real dev stack in BOTH languages, not just code-read:** clicked Rerun on a Hebrew conversation ("אירועים בחודש") -- the menu showed "הרצה מחדש" in the correct position, clicking it immediately opened the main pane on a fresh streaming answer to the source's exact first question ("מה קרה החודש?"), which completed against LIVE current data (period 2026-07-01–2026-07-31, matching this session's regenerated demo data, not a stale cached answer) and landed as a genuinely NEW sidebar entry ("אירועים של החודש", its own freshly-generated title) while the original "אירועים בחודש" conversation remained unchanged and in place further down the list; repeated on an English conversation ("Monthly AOV") and confirmed the same mechanism fires correctly ("What is our AOV this month?" resubmitted fresh). Drill threads were not separately live-tested (they belong to the source's own widgets and the rerun code path never touches them by construction -- no thread-related API call exists anywhere in `rerunConversation`), but this is a structural guarantee from the implementation, not an assumption glossed over. |
| `i18n_language_files_prompt.md` | **Infrastructure (item 1):** `frontend/src/locales/en.ts` (the source of truth, one typed namespaced object -- `common`/`chat`/`sidebar`/`menus.conversation`/`workspace`/`userMenu`/`messageActions`/`home`/`admin.users`) with `export type TranslationKeys = typeof en` (deliberately WITHOUT `as const`, so string properties widen to `string` rather than literal English types -- with `as const`, `he.ts` couldn't type-check at all, every Hebrew string would be "not assignable to type '<the English literal>'"; found and fixed this the hard way via ~80 real compiler errors on the first pass). `he.ts` declares `export const he: TranslationKeys = {...}`, so TypeScript's own excess/missing-property check on a typed object literal enforces structural parity at the BUILD level, exactly as asked. `locales/index.ts`'s `useT()` returns the whole per-language object (`LOCALES[lang]`, `lang` from the existing `useUiLang()`/`document.documentElement.lang` source -- no new plumbing) so call sites access nested paths directly (`t.sidebar.newConversation`) and a typo or renamed key fails the build rather than silently missing at runtime, matching the task's own stated reasoning for TS modules over JSON. Interpolated strings (counts, names) are functions, not templates -- same convention `chatCopy.ts`'s pre-existing `drillPlaceholder` already used. **Migrated the existing scattered dicts (item 1's second half):** `placeholderCopy.ts`'s `COMMON`/`USER_MENU`/`ROLE_LABEL`/`GEAR_MENU` and the standalone `feedbackCopy.ts`/`chatCopy.ts` all moved into the locale files and were DELETED (not left as dead duplicates) once their consumers were repointed; `admin/roleLabels.ts` (a THIRD, English-only copy of the same three role labels) also deleted once `UserRow.tsx`/`UserDetailDrawer.tsx` moved to `t.common.roles`. `placeholderCopy.ts`'s `DATA_SOURCE_GALLERY` deliberately stays put and unmigrated -- confirmed via reading `AddDataSourceModal.tsx` that it already reads only `.en` by a prior, still-current, explicit English-only decision for that one flow; moving it into the org-language-aware locale files would misrepresent it as something that follows the org setting. `loginCopy.ts` untouched (login stays English, per the task's own item 4) and `auditSentences.ts` untouched (its own bilingual template logic, a judgment call the task explicitly permits) -- both exactly as the task's own guidance anticipated. **Sweep -- app chrome (item 2):** `Sidebar.tsx`, `ConversationList.tsx` (+ its group-label constant moved out of `lib/conversations.ts`, which no longer carries its own English-only copy), `ConversationItem.tsx` (the reported kebab menu -- Rename/Pin chat/Unpin chat/Archive/Unarchive/Delete/"Delete this chat?"/Cancel -- plus its drill-down-follow-up-count tooltip, previously deliberately English-only per a NOW-SUPERSEDED comment that's been updated to explain why it's in scope now), `WorkspaceSwitcher.tsx` (the reported "Connected"/"Disconnected" + gear menu), `UserFooter.tsx`, `ChatInput.tsx`/`DrillChat.tsx` (chat + drill placeholders), `MessageActions.tsx` (Copy answer/Retry/Copy failed + the feedback popover), `HomeDashboard.tsx` (greeting, subtitle, all 3 brief chips' labels and dropdown content, section labels, period picker incl. presets/custom-range/from-to-month, "Ask SEMA why", discovery section, trending label) and `DailyBrief.tsx` (heading, "As of", "Tell me more", the quiet-day line) -- every one of these confirmed via live DOM inspection, not assumed from the diff. **Sweep -- admin (item 3, PARTIAL, see below):** `UsersScreen.tsx` fully migrated (breadcrumb, title, subtitle, invite CTA, search+role toolbar, column headers, empty states, last-admin notice), `UserRow.tsx` (identity block, role select, status, kebab menu, invited-row Resend/Cancel), `UserDetailDrawer.tsx` (every field label, status toggle, last-admin notice), `InviteModal.tsx` (title, all form labels/options, error text, footer buttons) -- chosen as the one admin screen because it's also where `popover_zorder_sweep_prompt.md`'s reported bug lived, so the same surface now demonstrates three fixes at once. **Scope decision, surfaced honestly rather than silently claiming full completion:** item 3 literally says "same treatment for ALL admin screens (nav items, Data sources, Semantic model tabs, Org settings, Audit log UI labels)" -- that remains undone. The admin nav item labels (Users and permissions/Home screen/Data sources/Semantic model/Organization settings/Audit log), the Data Sources screen, the Semantic Model screen, the Org Settings screen, and the Audit log screen's OWN chrome (its filters toolbar, category labels, drawer) are all still hardcoded English. Given the infrastructure (the genuinely hard, judgment-heavy part -- the locale file shape, the compile-time parity mechanism, the useT() pattern) is now built and proven correct in production, migrating the remaining screens is now mechanical repetition of the exact same pattern demonstrated here, not new design work -- a reasonable next queue item if the user wants full item-3 completion. **Guardrails (item 5):** `locales/locales.test.ts` recursively walks both trees and asserts (a) identical key sets, (b) identical value KIND per key (string vs. interpolating function) -- the runtime belt to the TS compile-time suspenders, catching what a hand-edit might slip past type-checking in one pass, (c) every string is non-empty in both languages AND actually differs between them (2 explicit, documented exceptions: an em-dash placeholder and an example email address, neither of which is linguistic content), (d) every interpolating function produces non-empty output in both languages across representative args. Hebrew quality followed the task's own house style: SEMA referenced in feminine where it appears ("לשאול את SEMA למה"), gendered UI verbs use the plural form for gender-neutrality ("נסו שוב"-style, e.g. "לחצו שוב כדי להסיר") rather than defaulting to masculine, sentence case, and phrasing modeled on the ALREADY-EXISTING Hebrew strings in the codebase (`תשובה טובה`/`תשובה לא טובה` etc., kept verbatim from `feedbackCopy.ts`) rather than literal machine translation. **Explicitly out of scope, not silently dropped:** calendar month-name formatting (`monthLabel` in `HomeDashboard.tsx`) stays `toLocaleDateString("en", ...)` -- a date-formatting-library concern distinct from copy strings, matching the spirit of "technical identifiers stay English"; `followUpsLabel` (`lib/format.ts`, used by `ChartRenderer`/`DataTable`/`KpiCards`/`RecommendedActions`) is deliberately UNCHANGED -- it labels widgets INSIDE an agent's answer, which follows the QUESTION's language per item 4's own rule, not the org chrome setting, and conflating the two would be exactly the bug item 4 warns against; role/data-scope CATALOG descriptions (the long sentences under each role/scope in `RoleTooltip`/`DataScopeControl`, e.g. "Can ask SEMA questions, view all business data...") come from `GET /api/admin/roles`/`/data-scopes` server responses and stay English -- localizing those is a backend i18n project, not a frontend locale-file migration. `npm run lint`/`build`/`test` (124, +4 net after deleting `chatCopy.test.ts`'s 4 tests and adding `locales.test.ts`'s 4) all green; `pytest` 733 passed + the same pre-existing timestamp-race flake in `test_org_settings.py` (zero backend changes this task, confirmed unrelated). **Verified live end-to-end against the real dev stack in BOTH languages** (not just type-checked): the org's language was already Hebrew from earlier session testing, which let the FULL sweep be confirmed live immediately -- conversation kebab menu (שינוי שם/הצמדת שיחה/העברה לארכיון/מחיקה), workspace switcher ("מחובר" for the exact reported "Connected" case, plus its listbox and gear menu), sidebar search/labels, the ENTIRE home dashboard (greeting, subtitle, all 3 brief chips, daily brief heading + "as of" + "tell me more", business overview, top recommendation + "ask SEMA why", start-a-conversation + discovery prompt, trending label), and the full Users admin screen including the invite modal and per-row kebab -- all read via live DOM `innerText`, not screenshots (this environment's screenshot tool is unavailable, per prior sessions' notes). Then flipped the org's `default_language` setting to English via the real Org Settings screen (discovered live that `document.documentElement.lang` only updates on next page load, not reactively -- matching a documented pattern from `chat_placeholder_language_prompt.md`'s own prior work, not a new bug), reloaded, and confirmed the identical surfaces render the ORIGINAL English strings verbatim (no regression) -- including that the drill-down-follow-up count's singular/plural branch works correctly in both directions ("1 drill-down follow-up" / "המשך אחד"). Restored the org's language back to Hebrew afterward (its state before this session), matching the demo-hygiene pattern established in `chat_placeholder_language_prompt.md`. |
| `users_pagination_prompt.md` | **Backend:** `sema_core/org_user_store.py`'s `list_users` factored its WHERE-clause construction into `_filter_clause` (shared with a new `count_users`, mirroring `audit_store.py`'s existing `_where_clause`/`COUNT(*)` pattern) and gained optional `page`/`page_size` kwargs -- `LIMIT`/`OFFSET` only applied when BOTH are given, so every existing unpaged caller (the last-active-admin count, the whole-org member/pending tally, `test_org_users.py`'s many `list_users(CID)` calls) keeps working unchanged; confirmed via the full existing suite passing untouched. `GET /api/admin/users` gained `page`/`page_size` (default 25 -- denser than the audit log's 50, per spec §8) and now returns `total` (the FILTERED count, for the pager), `page`, `page_size`, and `active_admin_count` (whole-org, via the store's existing `count_active_admins` -- needed because the last-admin guard must hold regardless of which page/filter is currently showing, not just the current page's rows) alongside the existing whole-org `member_count`/`pending_count`. **Frontend:** extracted a shared `components/admin/Pager.tsx` from the Audit log screen's own inline pager (exact same "Showing X-Y of Z" + prev/next markup) per the task's explicit "reuse its pattern and components, don't invent a second pager" instruction -- `AuditLogScreen.tsx` was itself migrated onto the new shared component (not left as the original), gaining a `hideWhenSinglePage` opt-in prop the Users screen sets (audit log keeps its own always-show-when-total>0 behavior unchanged). `useAdminUsers.ts` rewritten from "load the whole roster once, filter client-side" (its own docstring said pagination "isn't needed at this size" -- the assumption this task exists to invalidate) to server-side search/role-filter/paging mirroring `useAuditLog`'s already-proven shape (debounced search, filter-change resets to page 1). Optimistic mutations (role/status/data-scope) still patch the current page in place for instant feedback, then trigger a background `refresh()` once the request settles, so a role change that moves a row out of a role-filtered view (or a total that shifts) reconciles shortly after rather than silently drifting stale. `remove` handles the empty-last-page edge case explicitly (item 3's accept criterion): optimistic local removal, then if the page is now empty and isn't page 1, `setPage` back one (which re-fetches automatically via the existing page-change effect); otherwise a background refresh. Found and fixed one real latent bug while touching this: `AuditLogScreen.tsx`'s actor-filter dropdown called `api.admin.users()` with no args to get "the whole roster" for its options list -- true before this task (unpaged), but silently would have truncated to 25 users once the endpoint defaulted to paging; fixed by passing `page_size: 1000` explicitly (documented inline) rather than leaving a wrong assumption baked into an unrelated screen. **Tests:** 6 new backend tests in `tests/test_org_users.py` -- 3 store-level (`list_users` returns the exact right slice across 3 pages of 2, including a correctly-partial last page; unpaged omits both args and still returns everyone in one call; `count_users` matches `list_users`' own filtered length across no-filter/role-filter/search-filter) and 3 API-level (default response shape has `total`/`page`/`page_size` alongside the pre-existing counts; page 2 returns the disjoint remaining slice with a page_size below the 10-user seed so there's a real second page to check; `total` reflects the CURRENT filter, not the whole org, catching the exact bug class where a pager would offer pages that don't exist). Backend pagination MATH and "page reset on filter change" behavior are both covered (store/API tests for the former, `useAdminUsers`'s filter-triggered `setPage(1)` effect for the latter, mirroring `useAuditLog`'s identical, already-proven pattern) -- no new frontend hook-test infrastructure was added for the reset itself since none exists anywhere in this codebase yet (confirmed via search; the established convention per `chatCopy.test.ts` is testing pure logic, not hooks/rendered trees), so that piece is covered by live verification instead, see below. `pytest` (733 passed + the same pre-existing timestamp-race flake in `test_org_settings.py` documented in earlier queue entries, confirmed unrelated), `npm run lint`/`build`/`vitest` (124, unchanged) all green. **Verified live end-to-end against the real dev stack:** confirmed the actual network request carries `page=1&page_size=25`; confirmed the response body's `total`/`page`/`page_size`/`active_admin_count` all match the demo tenant's real 10-user roster (`member_count: 9, pending_count: 1, active_admin_count: 1, total: 10`); confirmed the pager renders NOTHING for this tenant (`hideWhenSinglePage` correctly hides it at 10 ≤ 25 -- the "no pager noise" accept criterion) rather than merely disabling its arrows; confirmed typing into the search box fires a debounced `?q=noa&page=1&page_size=25` request and the table narrows to the one matching row while the "9 members · 1 pending invite" subtitle stays exactly as it was (whole-org, not filtered, as designed). The >25-users paging-controls-actually-clicking scenario is verified by the new pytest slice tests (page-math correctness, the genuinely risky part) rather than by seeding extra users into the live demo tenant, per the task's own explicit instruction not to do that; the pager's rendering/click code itself is unchanged from `AuditLogScreen`'s pattern, which the earlier `table_pager_arrows_prompt.md`/`popover_zorder_sweep_prompt.md` work already exercised live. |
| `popover_zorder_sweep_prompt.md` | **Shared implementation:** extracted the sidebar conversation-menu's portal pattern (`sidebar_improvements_prompt.md`) into `hooks/usePopoverMenu.ts` (portal-ready viewport position from the trigger's rect with optional flip-up/forced-above placement and start/end/trigger-width alignment, outside-click+Escape dismiss across the trigger+portaled-menu pair via the existing `useDismissRefs`, Tab focus-trap via the existing `useFocusTrap`, close-on-scroll) paired with `components/PopoverMenu.tsx` (the actual `createPortal(..., document.body)` render, with optional `role`/`ariaLabel`/`dir` overrides). ConversationItem.tsx itself was refactored onto the new shared hook too (not just left as the reference), so there's exactly one implementation, not two that could drift. **Full audit, not just the reported instance:** grepped the whole frontend for `createPortal`/`role="menu"`/`role="dialog"`/`role="listbox"`/`MoreHorizontal`/`MoreVertical`/`useDismiss`/`useFocusTrap` (21 files) and read every match to classify. **12 real popover/kebab instances migrated** (before: hand-rolled `absolute`+`useDismiss`, clippable by any scrolling/z-index ancestor; after: portaled to `document.body`, `position: fixed`, immune to that class of bug) — `UserRow.tsx` Users-screen row kebab (**the reported bug**), `ConversationItem.tsx` sidebar kebab (dedup only, was already correct), `HomeDashboard.tsx` `BriefChip`×3-call-sites (alert/status dropdowns) and `PeriodPicker` (Business Overview period picker), `WorkspaceSwitcher.tsx` workspace listbox and gear/admin submenu, `UserFooter.tsx` user menu, `DataScopeControl.tsx` data-access editor popover and its `DataScopeLegend` info popover, `RoleTooltip.tsx`'s `RoleLegend` info popover, `MessageActions.tsx` feedback comment popover, `CopyButton.tsx` multi-action copy menu (charts/tables/KPIs throughout every answer). Found and fixed one real design bug in `PopoverMenu.tsx` during the sweep itself: it originally hardcoded `rounded-xl2`, but different call sites' ORIGINAL markup used different radii (`rounded-xl`, `rounded-lg`) — a caller's own radius class would have silently lost to the hardcoded one depending on Tailwind's generated CSS order (not JSX class order); fixed by not baking in a radius at all and having each of the 12 call sites specify its own, matching the original per-component value exactly. `BriefChip`'s migration is a deliberate, disclosed simplification: the pre-portal version forced mutual exclusion (only one of the 3 dashboard chips open at a time) via one shared dismiss ref on the whole row; each chip now owns its popover independently (via a `children: (close) => ReactNode` render-prop so an alert's "Investigate" action can still close its OWN chip before navigating) since the mutual-exclusion constraint added no behavioral value and doesn't fit a per-trigger portal's independent position calculation. **Considered and explicitly left unmigrated, with reasons** (per the prompt's own request to report before/after per instance): `RoleTooltip`'s hover tooltip (`role="tooltip"`) -- a fundamentally different pattern (always-mounted, opacity-toggled on hover/focus, no dismiss/focus-trap semantics), not a dismissable menu; `AuditLogScreen.tsx`'s pager -- same `MoreHorizontal`-adjacent icon family but the admin panel is English-only/LTR-always by established convention (`AdminPanel.tsx` has zero `dir` handling, confirmed via grep), so it's never wrapped in a clipping-risk RTL/scroll context the sidebar bug depended on; every `role="dialog" aria-modal="true"` modal/drawer (`AlertBuilder`, `HistorySlideOver`, `AuditEventDrawer`, `InviteModal`, `UserDetailDrawer`, `AddDataSourceModal`, `CsvUploadFlow`, `SheetsFlow`, `SaasRequestForm`, `SqlDbWizard`) -- a different UI pattern (centered/edge full overlays), not small trigger-anchored popovers subject to sibling-row or scroll-container clipping; `DataSourcesScreen.tsx`'s "report a problem" confirm -- inline-expanding card content, not an absolutely-positioned popover at all; the speculative "Data sources card menus"/"semantic model item menus/History actions"/"alert card menus" the prompt guessed might exist -- grepped for and confirmed NONE exist as popovers (reported honestly rather than inventing instances to migrate); Users-screen server-side pagination (`users_pagination_prompt.md`, item 24) -- not merged yet, so there's no pager instance there to fix. **Verified live end-to-end against the real dev stack, not just type-checked** (real DOM inspection via `getBoundingClientRect`/`parentElement`, not screenshots -- this environment's Browser-pane screenshot tool is unavailable per prior sessions' notes): all 12 migrated popovers open, portal to `document.body` with `position: fixed`, and land at sane on-screen coordinates -- specifically confirmed the REPORTED case (Users screen, last visible row "Ronit Shapira", not enough room below) flips upward and renders `{top: 416, bottom: 531.6}`, fully within the 720px viewport, versus its trigger at `{top: 612, bottom: 644}` -- exactly the "renders fully above the row" the accept criterion asks for. Also confirmed: the sidebar kebab still works post-refactor (no regression from deduping it onto the shared hook), the workspace switcher/gear/user-footer menus all open correctly upward from the sidebar bottom, all 3 dashboard `BriefChip`s plus `PeriodPicker` open with correct content, the Hebrew feedback comment popover opens with `dir="rtl"` and the correct Hebrew copy, and the chart's "Copy image / Copy underlying data" menu portals correctly. `npm run lint`/`build`/`vitest` (124, unchanged) all green. `pytest`: 728 passed + 1 pre-existing flaky timestamp-race failure in `test_org_settings.py` (documented flaky in earlier queue entries; passed on an isolated re-run, confirmed unrelated since this task made zero backend changes). |
| `table_pager_arrows_prompt.md` | Root-caused, not guessed: `AssistantResponseCard.tsx` wraps the whole answer body in `<div dir={dir}>` (line 153), and `DataTable.tsx`'s pager row (`flex items-center gap-1` containing the previous/label/next buttons) never re-isolated its own direction — so a Hebrew (`dir="rtl"`) answer visually mirrors the pager's flex order while the hardcoded `ChevronLeft`/`ChevronRight` glyphs stay fixed, landing "next" on the left pointing away from its own action. Confirmed via grep this is the ONLY affected pager: `AuditLogScreen.tsx`'s pager (same icon pair) is never inside an RTL container — the admin panel is English-only/LTR-always by established convention (`AdminPanel.tsx` has zero `dir` handling) — and Users-screen pagination (`users_pagination_prompt.md`, item 24) isn't merged yet, so there's no third instance to fix. `DataTable.tsx` is shared by both main-chat and drill-chat tables (`AssistantResponseCard` renders it in both paths), so one fix covers both without duplication. **Fix (option (a) from the prompt, matching the app's existing "digits/SQL/IDs stay LTR regardless of answer language" convention):** added `dir="ltr"` directly to the pager's wrapping `<div>` — the simplest robust isolation, no icon-swapping logic needed. **The bug was visual-only, not functional** — `onClick={() => instance.previousPage()}` was always on the LEFT-rendered button and `nextPage()` on the right in the DOM/React tree; only the CSS-driven visual position flipped under RTL, so before the fix a user would have clicked the correctly-positioned-looking-wrong button and gotten the right result with a confusing icon, not the wrong result. Verified this precisely: confirmed via live DOM inspection (`getBoundingClientRect`) that the previous/next buttons' rendered x-positions already matched their click handlers even before isolating direction (since `previousPage()`/`nextPage()` are wired to fixed DOM nodes, not swapped by CSS) — the icons/positions were what was wrong, not the wiring. Verified live end-to-end against the real dev stack (not just code-read): asked a fresh Hebrew question producing a 403-row table (9 pages @ 50/page) inside an RTL answer, confirmed via DOM query the pager's ancestor is `dir="ltr"` and Previous(x=337)/Next(x=438) sit in correct LTR order with Previous correctly disabled on page 1; clicked through all 9 pages one at a time (150ms apart, real click events) confirming "Page N of 9" advanced correctly at every step and Next correctly disabled exactly at "Page 9 of 9" with Previous enabled — the first/last-page disabled-state edge cases both correct. `npm run lint`/`build` (unchanged bundle size) and `npm run test` (124 vitest, unchanged) all green; no backend touched, so the prior 729-pass `pytest` run stands. |
| `demo_freshness_prompt.md` | Picked up from a prior session's paused research (see `demo-freshness-task-paused.md` memory) with the design decision already made: **complete-months-only**, not literal "yesterday" — `END_DATE` = last day of the last fully-complete calendar month before today, chosen as the lowest-risk option since the generator's existing architecture deliberately never produces a partial month. **Both generators** (`data/generate_data.py`, `data/insurance/generate_data.py`) gained a re-callable `_configure(end_date_override=None)` (global reassignment, called once at import for the default/env-var case and again in `main()` after `--end-date` CLI parsing — avoids parsing argv at import time, which would break under pytest) computing `START_DATE`/`END_DATE` and every date derived from them. `--end-date YYYY-MM-DD` (validated as a real month-end) and `SEMA_DATA_END_DATE` both override for reproducible pinned fixtures — confirmed pinning to the old hardcoded `2026-06-30` reproduces the exact original window byte-for-byte in both files. **Story-month constants**, split by what they need to mean: `MARCH_DIP_MONTH` (ecommerce) and `EVENT_MONTH` (insurance, the Jan weather event) stay pinned to the real calendar month (scan the window for `month==3`/`month==1`, most recent occurrence) since `MONTH_SEASONALITY`/`WINTER_MONTHS` are keyed by calendar month number and recur every year — a relative offset would decouple the story from that multiplier. `SUMMER_SALE_MONTH`/`PRICE_INCREASE_MONTH`/`EMAIL_LIFT_MONTH` (ecommerce) and `HEATWAVE_MONTH`/`RATE_ACTION_DATE` (insurance) always track `months[-1]`, the window's last month — this means "the Summer Sale" genuinely slides forward every regeneration (was June 2026, is July 2026 as of this run). Two real pre-existing bugs fixed while touching this code (both previously flagged in the paused research, now actually fixed): insurance's first-policy-inception floor was a stray hardcoded `date(2025,6,1)` instead of relative to `START_DATE` (now `FIRST_INCEPTION_END`, 17 months after `START_DATE`); the Jan-event/heatwave claim-date generation used `rng.integers(1,29)` for January (undercounting 3 days) and a hardcoded `date(2026, 1/6, ...)` regardless of the actual event month — both fixed with a `days_in_month()` helper and the real `EVENT_MONTH`/`HEATWAVE_MONTH`. Two NEW bugs caught during this task's own work (not pre-existing): the ecommerce campaign literally named `"Summer Sale 2026"` regardless of the actual sale year (now `f"Summer Sale {SUMMER_SALE_MONTH.year}"`), and an unguarded `date(SUMMER_SALE_MONTH.year, SUMMER_SALE_MONTH.month + 1, 1)` that would `ValueError` if the sale month ever landed on December (now a proper `month_end()` helper, same pattern already used elsewhere in the file). **Regeneration + reload:** both tenants regenerated with the new relative default (window 2025-07-01 → 2026-07-31 as of today) and reloaded into the live `sema_db`/`insurance_db` Postgres via the existing `data/load_data.py`/`data/insurance/load_data.py` (both already fully DROP+recreate+idempotent, no changes needed) — insurance's claims/policies date ranges confirmed to extend through the new `END_DATE`/`TODAY` via direct DB query. **`sql/semantic/knowledge.yaml`:** Black Friday and post-holiday-slowdown entries needed NO changes (their referenced calendar year, 2025/2026, is still inside the new window — verified via real weekday math, not assumed). The Electronics-price-increase note now tracks `PRICE_INCREASE_MONTH` (2026-07-01, was 2026-06-01) with freshly re-verified numbers. The checkout-outage and marketing-pixel-misfire entries are coincidental RNG artifacts with no engineered generator constant (confirmed in the prior session's research) — re-detected against the regenerated data (lowest-orders day in a stable non-holiday stretch: 2025-08-12, 30 orders vs. a ~49/day norm; a Google Campaign 3 week, 2025-09-08 to 2025-09-14) and the entries' descriptions now say explicitly that these dates are RNG-coincidental and must be re-detected on every regeneration, not assumed stable. **Tests:** investigated all 11 pytest files matching `2026-0[136]` before touching anything — confirmed via grep that NONE of them import `generate_data` or reference its constants; every hit is a self-contained mock/fixture date in tests that use fakes (per `evals/README.md`'s own architecture: pytest never touches the live DB), so nothing needed to change there. **`evals/golden/ecommerce.yaml`** DOES run against the live DB (real billed LLM calls) per its own README, so its hardcoded "June 2026" question text and values were genuinely stale after the regeneration — re-verified every assertion against the live DB and updated values/question text to July 2026 where the story's month moved (`summer_sale_bought_growth`, `summer_sale_aov_direction`, `electronics_price_increase`, `best_channel_june`, both drill-down language-anchoring cases), left assertions alone where they were already loose enough to keep passing (`worst_campaign`'s accepted-names list, `churn_risk_count`'s tolerance band), and added an explicit header note that this file needs re-verification again on every future regeneration — an accepted trade-off, not automated away. **`data/README.md`** (the evals answer key) fully re-verified against the live DB: revised intro explaining the new relative-by-default/`--end-date`-pinned design, full monthly revenue table, March-dip/VIP/churn-risk/Summer-Sale/price-increase/email-lift sections all recomputed from live SQL queries, not copied from memory. `pytest` (729 passed), `npm run lint`/`build` all green. **Verified live end-to-end against the real dev stack, not just unit-tested:** home dashboard's daily brief showed "As of Jul 31, 2026" (1 day behind the real `2026-08-01`, i.e. genuinely fresh, within the expected 1-31-day range this design accepts) with Revenue/Orders/AOV/At-risk-customers KPIs matching the freshly computed ground truth exactly ($1.19M +2.0%, 1,861 orders +24.0%, $639.36 AOV -17.7%, 1,791 at-risk); asked a fresh Hebrew relative-language question ("מה קרה החודש?" — "what happened this month?") in a new conversation and got a real, fully-grounded answer resolving to `Period: 2026-07-01 – 2026-07-31` with numbers matching the DB exactly — proving the actual accept-criterion behavior ("this month" questions no longer go empty) works, not just that the underlying data changed. Insurance tenant's claims/policies date ranges confirmed fresh via direct DB query (claims through 2026-07-31, policies renewing forward to 2027-08-01); its `/api/overview` KPI cards are empty, but confirmed pre-existing/unrelated (a home-config setup gap, not a data-freshness regression) rather than assumed fine. |
| `trend_line_charts_prompt.md` | The spec's own framing was stale — a `kind`/`type` field already existed (`Chart.kind: Literal["line","bar","grouped_bar","donut"]`, `api/models.py`) and the frontend already fully rendered `line` correctly in both single-series (`AreaChart`, soft fill + dots) and multi-series (`LineChart` via `color`) form (`ChartRenderer.tsx`) -- confirmed by reading it, not assumed, so item 2 ("frontend: render type: line") needed zero code. **The actual gap (item 1):** neither the system prompt's `- chart:` bullet (`sema_core/agent/prompts.py`) nor the `present_answer` tool schema's `kind` field (`sema_core/agent/response.py`'s `PRESENT_ANSWER_TOOL`) said ANYTHING about when to pick line vs. bar -- the tool schema's `kind` property had no `description` key at all, unlike every sibling field. Fixed both: the system prompt bullet and the tool schema's `kind` description now both state the same rule verbatim (x-axis is a date/period sequence -> `line`, even for a single category like "Accessories revenue by month"; categorical/entity x-axis -> `bar`/`grouped_bar`; share-of-whole -> `donut`), reinforced at the point the model actually reads schema field-by-field, not just argued once earlier in the prompt. **Item 3 (deterministic safety net):** new `_looks_like_date_axis(df, column)` in `response.py`, checking the column NAME (mirroring `queries.py`'s existing `_normalize_dates` convention: "month"/"day"/etc. are always time) and, for a differently-named column, the VALUES themselves -- either real `datetime.date`/`datetime` objects (confirmed via the research pass that Postgres `::date` columns come back from psycopg2 as dtype-`object` Python `date` instances, NOT `datetime64`, so a naive dtype check alone would miss the common case) or an ISO-shaped string (`TO_CHAR(...)`-formatted labels like "2026-01"). Wired into `build_response()`: a `bar`/`grouped_bar` chart whose `x` is date-like gets coerced to `line` (logged as `chart_kind_coerced`, `from_kind`/`to_kind`/`x_column`) regardless of what the model picked; `donut` is never coerced (no x-axis concept applies to it). **Item 4:** untouched by design -- old stored answers keep whatever `kind` they were saved with, no migration, since the coercion only runs at NEW-answer bind time. **Item 5 (evals):** `evals/run_evals.py` gained `expects_chart_kind: line\|bar\|grouped_bar\|donut` (documented in `evals/README.md`); `evals/golden/ecommerce.yaml` gained the exact Hebrew case from the user's original report (`הכנסות לפי חודש` -> line) plus its English mirror, and the categorical counterpart (`הכנסות לפי קטגוריה` -> bar) plus its English mirror, and the pre-existing `revenue_trend_renders_chart` case was tightened from a vague "line/bar" comment to an explicit `expects_chart_kind: line` assertion. 13 new backend tests: `tests/test_response_builder.py` (10 -- 5 unit tests directly on `_looks_like_date_axis` incl. the real-`datetime.date`-objects case the dtype-only approach would've missed, plus 5 integration tests through `build_response` covering bar-coerced-to-line-with-log-assertion, grouped_bar-also-coerced, categorical-bar-left-alone, donut-never-coerced, and line-left-alone-when-already-correct) and `tests/test_evals_scoring.py` (3, for the new `expects_chart_kind` assertion). `pytest` (729, +13 over the prior 716) -- 10 in `test_response_builder.py` + 3 in `test_evals_scoring.py` -- `npm run lint`/`build`/`vitest` (124, unchanged, no frontend changes needed) all green. **Verified against the real model, not just unit-tested:** ran all 5 chart-kind golden cases through the actual eval harness against the live Anthropic API -- both Hebrew and English trend questions (`הכנסות לפי חודש`, `Revenue by month`) correctly returned `kind=line`, both categorical questions (`הכנסות לפי קטגוריה`, `Show revenue by category as a chart.`) correctly returned `kind=bar`, 5/5 pass, and zero `chart_kind_coerced` log lines fired across any of them -- confirming the STRENGTHENED PROMPT GUIDANCE alone is now sufficient (the model picks correctly on its own), with the safety net proven independently and separately via the `FakeTools`-based unit tests (which deliberately force a wrong `kind` to prove the server-side coercion actually fires when the model doesn't comply) |
| `deployment_prep_prompt.md` | **Judgment call surfaced to the user first (item 3's hardest part):** the prompt asks to gate "mock-identity conveniences" out of production, but `sema_core/current_user.py`'s mock identity isn't a side convenience -- it's the ONLY identity mechanism today, backing every `/api/admin/*` route and every data-scoped route; disabling it in production would break the whole admin panel and chat until real auth ships (blocked on this very host existing). Asked the user directly; decision: **leave it active, but make the risk impossible to miss** -- a loud `logger.warning` on every production boot (api/main.py) plus a prominent warning at the top of `docs/deployment.md`, rather than either silently shipping it or breaking the app. **1 (container):** `api/Dockerfile.prod` (new, multi-stage: node stage builds the frontend, python stage serves both the API and the built static output via `api/main.py`'s new catch-all SPA route, registered LAST so every `/api/*`/`/healthz` route still matches first) -- the "API-served" option, matching the prompt's own stated preference (fewer moving parts, no cross-origin CORS) for a single-service pilot; the separate-static-service alternative is documented in `docs/deployment.md` §1 rather than built, since it needs no new code beyond what's already there (`VITE_API_URL` at build time + `SEMA_CORS_ORIGINS`). Real bug caught and fixed during a LIVE build, not assumed working: setting `NODE_ENV=production` before `npm ci` made npm skip devDependencies -- exactly where `vite`/`typescript` live -- so the build failed with "tsc: not found"; removed (vite build already produces a production build regardless of NODE_ENV). `frontend/src/lib/api.ts`'s `BASE` resolution changed from `VITE_API_URL || "..."` to an explicit `!== undefined` check, so a deliberately EMPTY build-time value (same-origin relative calls, what the Docker build now sets) isn't coerced back to the `localhost:8000` dev fallback -- truthiness would have silently broken the whole single-service option. New `api/Dockerfile.prod.dockerignore` (BuildKit per-Dockerfile convention) since the root `.dockerignore` deliberately excludes `frontend/` for the existing dev image, which this build also needs in its context. **2 (env contract):** `.env.example` rewritten grouped/commented with every var `sema_core/settings.py` actually reads (previously missing ~15: `SEMA_MODEL*`, `SEMA_MAX_*`, `SEMA_ANTHROPIC_*`, `SEMA_ROW_LIMIT`, `SEMA_STATEMENT_TIMEOUT_MS`, `SEMA_DB_POOL_MAX`, `SEMA_CORS_ORIGINS`, `SEMA_API_KEY`, `SEMA_CONVERSATION_DB`, `SEMA_HISTORY_TOKEN_BUDGET`, `SEMA_RETENTION_ENABLED`) plus the new `SEMA_ENV`/`SEMA_PUBLIC_URL`/`SEMA_AUTH_MODE` and placeholder `SMTP_*` (reserved for `auth_login_prompt.md` Part A, nothing reads them yet -- documented, not wired, per the prompt's own ask to include them anyway). `sema_core/settings.py` gained `env`/`public_url`/`auth_mode`/`cookie_secure` fields and `validate_for_production(s) -> list[str]`, a pure function returning EVERY problem at once (not fail-on-first) -- checks `ANTHROPIC_API_KEY`/`SEMA_SECRETS_KEY` (existence AND real Fernet-key validity, via the same `cryptography.fernet.Fernet` already a project dependency) /`SEMA_API_KEY`/`SEMA_PUBLIC_URL` presence plus both DB passwords not still being the well-known local-dev defaults. `api/main.py` calls it at MODULE IMPORT time (right after `logger = get_logger("api")`, before any store singleton is constructed) when `SEMA_ENV=production`, raising `RuntimeError` with every problem listed -- verified via a REAL container boot (see below), not just the pure function. **3 (dev/demo endpoint audit):** genuinely audited, not assumed -- confirmed via grep that no seed/reset HTTP endpoint exists anywhere (`OrgUserStore.seed_demo_users` is only ever called from a manual script and test fixtures, never a route) and that the frontend's `DevStatePanel.tsx` is already fully gated by Vite's `import.meta.env.DEV` (stripped at production build, no backend route to double-gate). Nothing else needed gating -- reported honestly rather than inventing routes to remove. **4 (CORS/cookies):** `SEMA_PUBLIC_URL` is folded into the `SEMA_CORS_ORIGINS` allowlist automatically (`dict.fromkeys` dedup, order-stable) so a deployer doesn't repeat one origin in two vars; `settings.cookie_secure` (true iff `SEMA_ENV=production`) exists as the one flag `auth_login_prompt.md`'s eventual session cookie will read -- no cookie is actually set anywhere yet, this is the configuration surface only, per the prompt's own explicit scoping. **5 (health + logs):** `GET /healthz` (root path, not `/api`, `include_in_schema=False`) -- `require_api_key` gained a path-based exemption (the only way to exclude one route from FastAPI's app-level `dependencies=[...]`, which has no per-route opt-out) since this is a platform liveness probe that can't be expected to send `X-API-Key`; reports `db_connected` + `version` (from `SEMA_VERSION`, "unknown" if unset). Kept separate from the pre-existing, still-gated `GET /api/health` (the frontend's own richer connection-indicator poll) rather than merging them. Added `tests/test_secrets_not_logged.py`: a real request cycle (healthz, a wrong-key rejection, a correct-key request, an admin route) through 4 configured secrets (Anthropic key, SEMA_API_KEY, DB password, Fernet key), grepping `caplog` (attached directly to the non-propagating `"sema"` logger, not the root) for every one -- none found, as expected given `obs.log_event`'s explicit-named-fields-only discipline, but now a regression guard rather than an assumption. **6 (persistence):** confirmed `var/` (SQLite state + logos) is the only stateful path -- EXCEPT a real gap found by reading the actual Publish/Restore code, not assumed: `sql/semantic/*.yaml` (both `sql/semantic/` and `sql/insurance/semantic/`) are rewritten on disk at runtime by `sema_core/semantic_editor.py`'s `write_atomic`, masked in dev only because the whole repo is bind-mounted. Fixed by adding a second named volume (`sema_semantic_prod` at `/app/sql`) in `docker-compose.prod.yml`, not just documented as a caveat. **7 (docs):** `docs/deployment.md` (new) -- the mock-identity warning up front, both container options, the full required-var table, the persistence section explicitly calling out the semantic-YAML gap and how it's addressed, CORS/cookies, healthz, and a post-deploy smoke-test checklist; `README.md` gained a "Deployment" section (after Quickstart) pointing to it, and a `docs/` project-structure line. **8 (tests):** 22 new backend tests -- `tests/test_settings.py` (14: env/cookie_secure/auth_mode defaults, `validate_for_production` for each individual check plus "reports every problem at once", and 3 REAL subprocess tests -- `python -c "import api.main"` with controlled env -- proving the module-import-time raise actually fires in production with missing vars, doesn't in dev, and succeeds in production once everything is set; done via subprocess rather than an in-process reload since `api.main` has real import-time side effects that would corrupt the shared test-session module cache), `tests/test_healthz.py` (7: shape, the no-auth exemption specifically proven alongside a sanity check that OTHER routes still enforce the key, db-connectivity reporting, version-from-env, schema exclusion), `tests/test_secrets_not_logged.py` (1, see above). `pytest` (716, +22, the previously-flaky `test_org_settings.py` timestamp-race test did not reproduce), `npm run lint`/`build`/`vitest` (124, unchanged) all green. **Verified live end-to-end, not just unit-tested:** built the actual `api/Dockerfile.prod` image with real `docker build` (catching the NODE_ENV bug above); ran it standalone and confirmed `/healthz` (db_connected reflects real reachability), `/` (serves the built `index.html`), a real JS asset (correct `text/javascript` content-type), and the SPA fallback for an unknown client-side route all work; confirmed production mode with vars deliberately missing refuses to boot with the exact expected multi-line error, AND boots successfully end-to-end (including the mock-identity warning appearing in the logs) once every required var is set, with `SEMA_API_KEY` then correctly 401'ing an unkeyed request and accepting the right one; ran the FULL `docker-compose.prod.yml` stack under an isolated project name (`SEMA_HOST_PORT` env var added to the compose file so it can run alongside the dev stack's own port 8000 without conflict -- a real fix, not a test-only hack), created a real conversation through it, then proved persistence survives not just a container `restart` but a full `down`+`up` (containers destroyed and recreated from the image) -- the conversation, and the seeded `sql/semantic/*.yaml` files, both survived on the named volumes alone. All test containers/images/volumes/temp env files cleaned up afterward; confirmed the pre-existing dev stack (`sema-api`/`sema-frontend`/`sema-postgres`) was undisturbed throughout |
| `sidebar_improvements_prompt.md` | **1 (resizable width):** `Sidebar.tsx` gained a self-contained width state (`MIN_WIDTH=220, MAX_WIDTH=420, DEFAULT_WIDTH=288` -- the old fixed `w-72`), persisted to `localStorage["sema:sidebar:width"]`; the `<aside>`'s fixed `w-72` class became an inline `style={{width}}`. A drag handle (pointer events only, no dnd library) is an absolutely-positioned `w-2` hit-area straddling the physical right border (never `end-*` -- the sidebar never mirrors, matching `ConversationItem.tsx`'s own established physical-positioning convention), with a thin visible line that highlights on hover/focus; drag computes width from the pointer's horizontal delta since drag-start, clamped every frame. Double-click resets to the default; Arrow/Home/End keys also resize (`role="separator"`, `tabIndex=0`) since keyboard support was cheap to add alongside pointer events. Applies to both the desktop persistent sidebar and the mobile drawer (same shared component). Verified live via dispatched PointerEvents (computer/screenshot tools aren't available in this environment): a 63px drag delta produced an exact 63px width change; dragging 500px past the max clamped to exactly 420px; double-click reset to exactly 288px and updated localStorage; a width set directly in localStorage (350) was correctly restored after a full page reload. **2 (kebab menu z-order):** `ConversationItem.tsx`'s dropdown is now rendered via `createPortal(..., document.body)` instead of as an absolutely-positioned child of the row (which was being clipped/overlapped by later sidebar rows and the Archived toggle's own stacking context) -- position computed from the trigger's `getBoundingClientRect()` at open time (`position: fixed`, `z-50`), with a flip-up estimate (`MENU_EST_HEIGHT=190`) when there isn't room below. New `useDismissRefs` in `useDismiss.ts` (checks containment against TWO refs -- trigger + portaled menu -- since a single-ref check would treat every click inside the now-non-descendant menu as "outside"; purely additive, the original single-ref `useDismiss` is untouched and still used everywhere else). Reused the existing `useFocusTrap` hook (already in the codebase for DataScopeControl) on the portaled menu so Tab-cycling and initial autofocus still work despite the menu no longer being a DOM descendant of the trigger -- a real regression risk of portaling that would otherwise have silently broken keyboard navigation. Closes on scroll (position is viewport-coordinate, so it would visually detach if the list scrolled under it -- closing is simpler and more robust than tracking position every frame) in addition to the existing outside-click/Escape. Verified live: `menu.parentElement === document.body` confirmed, `position: fixed`/`z-index: 50` confirmed; Escape closes; focus lands on the first menu item on open (focus-trap working); scrolling the list closes the menu; forcing a small viewport (800×320) with the trigger near the bottom flipped the menu upward and kept it fully within `[0, viewportHeight]` (previously would have opened downward and been clipped/cut off). **3 (drill-count badge):** `sema_core/conversation_store.py`'s `list_conversations` gained a `drill_count` correlated subquery (`COUNT` over `thread_messages JOIN threads WHERE conversation_id = c.id AND role='user'`) -- same single-query shape as the existing `message_count` subquery, so listing conversations stays one query, no N+1 per-thread lookup; `api/models.py`'s `ConversationSummary` and the frontend's mirrored `lib/api.ts` interface both gained `drill_count: number` (default 0), flowing through the existing `ConversationSummary(**c)` dict-unpack with zero extra mapping code. `ConversationItem.tsx` renders the existing `ThreadBadge` pill (same "existing badge/pill pattern" already used for a widget's own follow-up count, for visual consistency) after the title, `shrink-0` so the title's own `truncate` always gives way first -- hidden entirely via `ThreadBadge`'s own `count===0` early-return; native `title` tooltip + an `aria-label` extension on the row (`"{title}, {N} drill-down follow-ups"`) for non-visual access. Kept the tooltip/aria-label ENGLISH-ONLY rather than the bilingual EN/HE text the prompt's own UI bullet suggested: AGENTS.md documents an explicit, enumerated "UI is English, always... EXACTLY two strings change [on Hebrew org language]" MVP decision (mirrored in `chatCopy.ts`'s own "exactly these three, everything else stays English" header), and this badge isn't one of them -- extending that enumerated exception for a tooltip felt like a policy call worth surfacing rather than silently deciding; noted in a code comment. 2 new backend tests (`test_list_conversations_aggregates_drill_count_across_threads` -- 2 turns on one widget's thread + 1 on a different widget's thread = 3, proving it's a true cross-thread aggregate and not per-thread `turn_count`; `test_list_conversations_drill_count_zero_when_no_drills`), added to `test_threads_api.py` alongside the existing thread tests it's a natural extension of. `pytest` (694, +2), `npm run lint`/`build`/`vitest` (124, unchanged) all green. Verified live against the real dev stack's existing drill history (not synthetic): `GET /api/conversations` correctly returned non-zero `drill_count` (1-3) for 12 real pre-existing conversations with drill threads; found "הכנסות השבוע" (drill_count=2) via the sidebar's own search, confirmed the badge renders "2" with the correct tooltip and combined Hebrew-title+English-count `aria-label`, and confirmed the row's `truncate`/`overflow:hidden`/`text-overflow:ellipsis` and fixed `h-10` row height are unaffected by the new badge sibling (RTL title + LTR badge coexist correctly, no wrapping) |
| `bugfix_batch_prompt.md` | **Bug 1 (admin panel ignores wide screens):** staged `max-w-*` container applied to all 6 admin screens, categorized by content type per the prompt's own "tables get the width, text blocks keep a sane max" distinction rather than one blanket value — `UsersScreen.tsx`/`AuditLogScreen.tsx` (table-heavy) get the exact `max-w-3xl xl:max-w-5xl 2xl:max-w-6xl` App.tsx already uses for the main chat; `SemanticModelScreen.tsx` (list+editor two-pane) `max-w-4xl xl:max-w-6xl 2xl:max-w-7xl`; `HomeConfigScreen.tsx` (settings+live-preview two-pane) `max-w-5xl xl:max-w-7xl 2xl:max-w-screen-2xl`; `DataSourcesScreen.tsx` (card grid) `max-w-4xl xl:max-w-5xl 2xl:max-w-6xl`; `OrgSettingsScreen.tsx` (form-heavy, deliberately conservative per the prompt's explicit "don't stretch form labels" carve-out) only `max-w-4xl 2xl:max-w-5xl` — individual fields already cap at `max-w-sm` so this only gives its 2-col card grid a bit more room, never stretches a label/hint line. No narrow-width behavior changed (only the upper bound moved), so no regression risk there. **Bug 2 (drill-down summary renders English for a Hebrew question):** root-caused via live testing, not guessed — `sema_core/agent/prompts.py`'s drill-down `[SEMA-CONTEXT]` (`build_drill_context`) always carries the widget's title/detail as a hardcoded ENGLISH literal regardless of question/UI language (e.g. KpiCards.tsx's `detail: current value ${valueText}...`), and a plain "same language as the question" instruction proved genuinely unreliable under that competing signal: a strengthened, explicit prose-only rule (anchoring to "THIS TURN's question, not the context block, not your own last answer") still measured 2/3 and 0/3 across live model calls in the two directions -- a real, repeatable bias toward whichever language the CONTEXT/parent turn was in, not sampling noise. Fixed by escalating from prose-only to a server-computed, deterministic per-turn fact: `build_user_message` (prompts.py) now detects the CURRENT question's script (Hebrew-char-ratio heuristic, `_question_script_hint`) and prepends an explicit `(This question is written in {Hebrew|English}. Answer EVERY field ... regardless of what language the context block above or any earlier turn ... used, even your own last answer.)` line immediately inside `[USER-QUESTION]` itself -- attached at the point of use rather than argued for earlier in the system prompt. Verified live end-to-end (real billed model calls, not assumed): 6/6 across both directions with the hint (previously 2/3 / 0/3 with prose alone), plus the two new golden-eval cases below run through the ACTUAL harness code path, both PASS. One self-inflicted bug caught and fixed during this: the hint's own wording originally contained the literal substring `[SEMA-CONTEXT]`, which broke the envelope's one-occurrence-per-delimiter invariant (`test_forged_envelope_delimiters_are_stripped_from_question` / `test_drill_context_fields_cannot_escape_the_block` caught it immediately) -- reworded to "the context block above" with no bracket tokens. Eval infra: `evals/run_evals.py` gained `drill:` case support (builds a real prior-turn `history` + `internal_context` via the actual `build_drill_context`, never a hand-written context block) and a `summary_language: he|en` assertion (`_text_language`, same crude-script-ratio heuristic, documented in `evals/README.md`); `evals/golden/ecommerce.yaml` gained the two required cases (`drill_summary_hebrew_question_over_english_parent`, `drill_summary_english_question_over_hebrew_parent`), both English-context-text in both directions (matching the real frontend's actual behavior) to isolate language-anchoring specifically; `tests/test_evals_scoring.py` gained unit tests for `_text_language` and the new assertion (pure logic, runs in ordinary CI per the file's existing convention). **Bug 3 (chart value labels glued to bar tops):** root-caused precisely — `LabelList`'s `offset` prop is silently ignored once a custom `content` render function is supplied (Recharts, confirmed empirically live: bumping `offset` 7→16 produced zero visual change), so the "fix" the labels already had was a no-op; the actual gap was a live-measured **-2.67px overlap** (glyph ascender into the bar). Fixed with a genuinely shared constant, `BAR_VALUE_LABEL_OFFSET = 8` in `lib/chart.ts`, applied manually inside `ChartRenderer.tsx`'s `valueLabel` renderer (`y={ly - BAR_VALUE_LABEL_OFFSET}`) instead of the ineffective prop — verified live: consistent ~5.3px gap across every bar in a 6-bar chart, both in the app's default USD formatting and after temporarily switching the org to ILS (`₪` suffix, reverted after) to confirm currency formatting doesn't disturb the fix; digits stayed LTR throughout (Hebrew question, RTL chrome). No clipping observed on the tallest bar (niceAxis's existing rounded-up headroom absorbs the added 8px). Also fixed in the same pass (found via direct user report mid-session, same file/family of bug): bar value labels were x-centered on the bar's LEFT EDGE, not its center (`valueLabel` read `props.x` but never added `props.width / 2`) — fixed alongside, verified live (bar center 86.26 vs. label center 86.37, sub-pixel match) — and an RTL chart-header layout bug (title should sit on the physical right / "Ask about this" on the physical left for an all-Hebrewanswer, chart plot itself staying LTR) fixed via a `flex-row-reverse` toggle keyed off the answer's `dir`, verified live (`flexDirection: row-reverse` + correct `pr-9`/no-clearance swap). Table headers (`DataTable.tsx`) also gained Hebrew labels for the app's known schema/semantic-layer column vocabulary (new `lib/columnLabels.ts`, ~50 entries from `sql/schema.sql` + `sql/semantic/*.yaml` aliases, falls back to prettified English for anything outside that set) — found via the same user report, same root cause family (RTL answer-screen polish), fixed alongside rather than deferred. And the "table renders twice" complaint from that same report was root-caused to the AGENT duplicating a markdown table inside `insight_text` on top of the real `table` binding — fixed with an explicit prompt rule (never render a markdown table for data already bound to `table`) rather than a frontend workaround, per the same "fix at the agent layer" philosophy as bug 2. `npm run lint`/`build`/`vitest` (124, unchanged) and `pytest` (692, the one previously-flaky `test_org_settings.py` timestamp-race test did NOT reproduce on the final run) all green; verified live end-to-end in the browser (Docker hot-reload dev stack) for bugs 1 and 3, and via real billed model calls (not mocked) for bug 2 |
| `data_sources_cleanup_prompt.md` — items 6 + 6a completion | Added after the main cleanup run closed out; picked up immediately after. `sema_core/connector_catalog.py`: `sap_b1` (kind `saas_request`, grouped into Business systems — reuses the generic `saas_request` branch in `AddDataSourceModal.tsx`'s `onCardClick`, so it opens the exact same request-form flow as Priority ERP with zero new frontend routing code) and `sharepoint` (kind `light`, grouped into Files and sheets — falls through to the same not-yet-wired-flow placeholder toast google_drive already gets, again with no new routing code, just a new catalog row). Deliberately named `sap_b1` (not `sap`) and labeled "SAP Business One" per the prompt's explicit instruction not to imply a generic/S4HANA SAP connector. `frontend/src/components/admin/AddDataSourceModal.tsx`: added `FolderOpen` icon mapping for `sharepoint`, reused the existing `FileSpreadsheet` icon for `sap_b1` (same icon Priority/Salesforce already share — consistent with "icon represents the category, not the brand" convention). `frontend/src/lib/placeholderCopy.ts`: also fixed the `he` half of the dead (`AddDataSourceModal` reads only `.en` since the English-only decision) `connectorUnlocks` dict, which still had the pre-cleanup `shopify`/`google_analytics`/`meta_ads` keys and was missing `google_drive` — brought both language blocks back into 1:1 parity (postgres/mysql/mssql/priority/salesforce/sap_b1/google_sheets/csv/google_drive/sharepoint) rather than carrying that drift forward. `npm run lint`/`build`/`vitest` (124, unchanged) and `pytest` (687) all green. Verified live: gallery now shows exactly 3 groups / 10 cards (SQL databases: Postgres/MySQL/MSSQL, Business systems: Priority/Salesforce/SAP Business One, Files and sheets: Sheets/CSV/Drive/SharePoint); clicking SharePoint shows the placeholder toast with no flow opening; clicking SAP Business One opens the real "Request SAP Business One" non-credential form (closed without submitting, confirmed via direct DB read afterward that `client_connections`/`source_requests`/`uploads` are still all empty for both tenants — no stray test row left behind this time) |
| `chat_placeholder_language_prompt.md` | New `frontend/src/lib/chatCopy.ts` — the ONE place all 3 org-language-following strings live: `CHAT_PLACEHOLDER` (en/he dict), `NEW_CONVERSATION` (en/he dict), `drillPlaceholder(lang, shortTitle)` (a function, since it interpolates the widget title). All three read `useUiLang()` (the same `document.documentElement.lang` source `org_settings_gapfix_prompt.md` already wired). **Main chat input:** `ChatInput.tsx` now computes its own default placeholder from `CHAT_PLACEHOLDER[lang]` instead of a hardcoded English literal (3 call sites: the textarea's `placeholder`, both branches of its `aria-label`) — App.tsx never passed an explicit `placeholder` prop, so this is what it was implicitly relying on. **DrillChat input:** replaced the placeholder's PRE-EXISTING `widget.dir`-based bilingual text (which mirrors the ORIGINAL triggering question's language, a separate and deliberately untouched feature — see the `DrillContext.dir` docstring) with `drillPlaceholder(lang, shortTitle)`; only one placeholder variant exists in the code (no separate new-thread/resumed-thread text), so there was only one string to convert — confirmed via the widget's own `initialInput`/`threadId` branches, neither changes the placeholder itself, only pre-fill/history-load. Left every OTHER `widget.dir`-driven string (STARTERS, the "Ask a follow-up about…" empty-state line, RTL alignment) exactly as-is, since the prompt only scoped the ghost placeholder. **"New conversation":** both occurrences updated — the real button in `Sidebar.tsx` and the empty-state hint's embedded reference in `ConversationList.tsx` (the surrounding English sentence "No conversations yet. Start one with ___." stays English by design; only the quoted label itself switches, wrapped in `dir="auto"` so the two languages don't visually clash mid-sentence). **`dir` handling (item 1), the one non-obvious part:** `dir="auto"` on the textarea does NOT reliably RTL-render *placeholder* text in this browser engine (verified live — Chromium here does not implement the HTML spec's empty-value-falls-back-to-placeholder clause for `dir="auto", unlike Firefox), so the textarea's `dir` is now `value ? "auto" : (lang === "he" ? "rtl" : "ltr")` — a static direction matching the placeholder's own language while empty, `"auto"` (unchanged, spec-correct) once there's real typed content, confirmed live: an English question typed into a Hebrew-chrome input still renders LTR. Added `frontend/src/lib/chatCopy.test.ts` (4 tests, mirroring `loginCopy.test.ts`'s pure-dictionary-assertion pattern — this codebase has no component-render test infra, confirmed again via search, so per established convention the logic itself is what's tested, not a rendered tree). `npm run lint`/`build`/`vitest` all green (124 tests, +4 new); no backend changes, so `pytest`'s prior 687-pass run stands. Verified live end-to-end against the real dev stack: toggled the org language to עברית in Organization settings, hard-reloaded (confirmed `document.documentElement.lang === "he"` — the org-settings fetch is once-on-mount, not live-reactive, matching existing behavior), confirmed the main input's placeholder read "אפשר לשאול על הכנסות, לקוחות וקמפיינים..." with `dir="rtl"` (computed direction confirmed via `getComputedStyle`, not just the attribute) while empty, switched to `dir="auto"`/LTR the instant English text was typed, "New conversation" read "שיחה חדשה", and opening a drill panel (revenue KPI card) showed "אפשר להמשיך לשאול על Revenue · Jun 2026..." also `dir="rtl"` — both inputs switched together as required. Confirmed nothing else on the page changed language (dashboard headings, KPI labels, daily brief, all still English) EXCEPT two pre-existing, already-bilingual surfaces from `product_placeholders_prompt.md` (the gear menu's "Manage organization"/"ניהול הארגון" and the sidebar role label) that were already `useUiLang`-driven before this task and are untouched by it — not a regression, out of scope. Reset the org language back to English afterward (same demo-hygiene pattern `org_settings_gapfix_prompt.md` used) and confirmed via a final reload: `document.documentElement.lang === "en"`, placeholder back to "Ask about revenue, customers, campaigns...", `dir="ltr"` |
| `data_sources_cleanup_prompt.md` | **Purge (item 1):** the 3 leftover test artifacts from the earlier `data_sources_add_prompt.md` live-verification pass — all `client_id='ecommerce'` only, `insurance` had zero rows in any of the 3 stores — deleted directly from the live dev DB: `client_connections` row "Warehouse Postgres" (fingerprint 3718), `source_requests` row "Main Priority instance", and `uploads` row `orders_sample.csv` (its backing Postgres table `uploads.orders_sample_4ada0bc2` dropped via the existing `sema_core.file_upload.drop_upload_table`, run inside the `sema-api` container so it hit the real analytics Postgres; the 3 SQLite rows deleted directly from `var/sema_state.db`, bind-mounted host↔container so no restart was needed). No seed/reset script ever created these rows (confirmed via repo-wide grep) — they were purely a manual artifact of that earlier live-verification session, so nothing needed to change to stop them recreating. **"Interest" records (item 1):** confirmed `POST .../interest` never writes a `source_requests` row (audit-log + platform-log only) — nothing to purge there. **Item 2 (priority/salesforce never render as cards):** already true by construction — `sema_core/data_sources.py`'s `client_sources()` only ever returns what's in `config/clients.yaml`'s `data_sources` block (postgres-only for both tenants today), so a catalog-only connector can only ever produce a card via a `source_requests`/`client_connections` row, never from the catalog itself; no code change needed. **Gallery (items 5-9):** `sema_core/connector_catalog.py` gained a `google_drive` entry (kind `light`, `availability: available` — grouped normally under Files and sheets, but its add-flow isn't built yet) and moved `shopify`/`google_analytics`/`meta_ads` into a new `_INACTIVE_CATALOG` list (never returned by `get_catalog()`, so the "coming soon" group now naturally renders nothing and never appears; still resolvable via `get_connector()` for any future re-activation or historical audit-log label lookups, one-line move back per the module's existing MYSQL_AVAILABLE-style feature-flag convention). `frontend/src/lib/placeholderCopy.ts`: `groups.easy` renamed "Easy sources"→"Files and sheets" (en) / "מקורות קלים"→"קבצים וגליונות" (he, kept in sync though unused per the login-page precedent of not deleting dead bilingual entries); removed the now-orphaned `shopify`/`google_analytics`/`meta_ads` keys from the dead (already-unused-by-the-modal) `connectorUnlocks` dict, added `google_drive`. `AddDataSourceModal.tsx`: per AGENTS.md's "UI is English, always" decision, now always reads `DATA_SOURCE_GALLERY.en` and `c.unlocks.en` instead of `useUiLang()` (removed that import/hook entirely from the component); `onCardClick` restructured so any "available" card whose flow isn't wired to a real handler (currently just Google Drive) falls through to the same placeholder toast a not-yet-available card gets, rather than silently doing nothing; added a `HardDrive` lucide icon mapping for `google_drive`. `DataSourcesScreen.tsx`'s "Add data source" button was also switched from `DATA_SOURCE_GALLERY[lang].addButton` to a literal English string — same principle, and it was the only bilingual leftover on an otherwise all-English screen (also drops its now-unused `useUiLang` import). **Item 4 (test dependency check):** searched the whole suite for any test asserting on the seeded demo rows — none found; `tests/conftest.py`'s `_isolated_data_sources` fixture already points `client_connections_store`/`source_requests_store`/`uploads_store` at fresh per-test tmp-path SQLite files, fully isolated from `var/sema_state.db`, so no test was ever coupled to the live demo data. The two catalog tests that touch shopify/the full connector set (`test_catalog_endpoint_lists_every_connector`'s subset assertion, `test_interest_recorded_for_coming_soon_connector` posting `connector_type: "shopify"`) both still pass unchanged — the former only asserts a subset, the latter only needs `get_connector` to resolve a label, which it still does via `_INACTIVE_CATALOG`. Full suite green (687 passed); `npm run lint`/`build`/`vitest` (120 tests) all green. Verified live in the browser against the real dev stack: E-Commerce tenant's Data sources screen now shows exactly one PostgreSQL card (Healthy, 6 entities, 30-day data age) with no leftover test cards; the Add gallery opens in English with exactly the 3 required groups (SQL databases / Business systems / Files and sheets) and 8 cards total, no Coming soon group; clicking the new Google Drive card shows the placeholder toast and opens no flow, with no console errors. Insurance tenant's underlying data was confirmed clean via direct DB inspection (zero rows in all 3 stores, always was) rather than live-browser-verified per-tenant, because of a pre-existing, already-documented limitation noted in `client_panel_screens_prompt.md — Phase 4`'s own queue entry: the admin panel's mocked identity isn't workspace-aware, so the Data sources screen keeps showing the ecommerce tenant's cards even after switching workspace in the sidebar — confirmed still true (unrelated to this change) and out of scope to fix here |
| `greeting_user_name_prompt.md` | Two frontend-only fixes, both fully covered (the file was updated mid-queue to add fix 1 on top of the originally-scoped fix 2; both are done). **Fix 1 (login screen always English):** `LoginPage.tsx` no longer calls `detectBrowserLang(navigator.language)` — `lang` is now the literal constant `"en"`, so `dir`/`LOGIN_COPY[lang]` and every child (`EmailStep`/`CodeStep`/`ErrorScreen`) resolve to English/LTR regardless of browser language; `detectBrowserLang` and `LOGIN_COPY.he` are left in `lib/loginCopy.ts` untouched (dead code for now, per the prompt's explicit "don't delete" instruction, in case per-org login language is reinstated later) — `loginCopy.test.ts` needed no changes since its assertions are pure dictionary/function checks, not language-selection behavior, and there is no component-render test for `LoginPage.tsx` to begin with (confirmed via search — this codebase has no `@testing-library/react`/jsdom render-test infra for any component, consistent with `auth_login_prompt.md` Part 0's own note). The tagline under the logo (`EmailStep.tsx`'s `{t.headline}`) changed from "Your organization's business advisor" to the literal brand phrase **"AI Business Advisor"**, matching the exact string already used in `Sidebar.tsx`'s footer and the page `<title>`. **Fix 2 (greet the person):** `App.tsx` fetches the mock identity once via `api.admin.me()` (same endpoint `UserFooter.tsx`'s sidebar row already reads, fetched independently rather than lifting shared state — cheap local SQLite lookup, matches this app's established per-component-fetches-what-it-needs convention) and derives `userFirstName` as the first whitespace-separated token of `me.name`, falling back to `null` (not the raw email) when the name is missing or the identity doesn't resolve; `HomeDashboard.tsx` renders `{userName || clientLabel}` wrapped in `dir="auto"` (a Hebrew display name inside the English greeting still renders correctly) so the org name is the fallback exactly as before, never dropped — it still appears, once, in the sidebar's own org row, not duplicated in the greeting. The admin panel's home-config live-preview call site (`HomeConfigScreen.tsx`) passes `userName={null}`, deliberately keeping its pre-existing org-name-only preview behavior (no identity context available there, and a preview showing "Miri" would be misleading for whoever's actually configuring it). No backend changes; `npm run lint`/`build`/`test` (111 vitest, unchanged — no existing test asserted the old org-name greeting text, confirmed via search, so none needed updating) all green. Verified live in the browser: `/login` renders "AI Business Advisor" with `document.querySelector('[dir]').dir === "ltr"` confirmed via a fresh tab (the app's own chrome language was Hebrew at the time, proving the hardcoding actually overrides it, not just coincidentally already English); a fresh (non-restored-from-localStorage) home dashboard shows "Good afternoon, Miri" with "E-Commerce" still present and unchanged in the sidebar |
| `answer_feedback_prompt.md` | New `sema_core/feedback_store.py` (`TurnFeedbackStore`, table `turn_feedback` UNIQUE(conversation_id, turn_index), upsert via `ON CONFLICT DO UPDATE` mirroring `semantic_store.save_draft`'s composite-key pattern; `clear()` deletes the row outright rather than storing a null rating — "no feedback" and "explicitly cleared" aren't worth distinguishing once the rating's gone). `api/models.py`: `TurnFeedback {rating, comment}`, `FeedbackRequest {conversation_id, turn_index, rating: Literal["up","down"]|None, comment ≤500 chars, client_id}`, `ConversationMessage` gained an optional `feedback` field (populated only on `ConversationDetail`, never `ThreadDetail` — drill/thread turns are explicitly out of scope per the prompt's item 5). `api/main.py`: `POST /api/feedback` sits at the SAME auth tier as the chat endpoints themselves (`_resolve_client` + `current_org_user`, NOT `require_client_admin` — a viewer/analyst must be able to rate an answer, not just admins); `rating: null` clears and returns `null`, otherwise upserts and returns the saved `TurnFeedback`; `get_conversation` now walks the raw message list tracking a running `turn_index` (incremented on each `user`-role row, exactly matching the client's own `turnsFromDetail` counting) and attaches the matching feedback row to each assistant message. Frontend: `ChatTurn` gained an optional `feedback` field; `turnsFromDetail` (`lib/conversations.ts`) carries it over from the assistant message; `useChat` gained `setFeedback(index, feedback)` for the optimistic local update (also means a completed turn's feedback round-trips through the existing `persistKey` localStorage cache alongside the rest of the turn). `MessageActions.tsx` rewritten from the `product_placeholders_prompt.md` "local flash, saves nothing" placeholder into the real thing: a rating click is optimistic + fire-and-forget (posts in the background, reverts SILENTLY on failure per the prompt's own "never block the UI... on failure revert silently and show nothing" — explicitly captures the pre-click `{rating, comment}` as an explicit `revertTo` value rather than reading it back off React state, so a revert is correct even if something else changed the state while the request was in flight); switching (up↔down) or re-clicking the same thumb (clear, `rating: null`) both send immediately; thumbs-down opens a small `useDismiss`-based inline popover (optional one-line comment + Send/Skip) whose Send is a SEPARATE follow-up POST updating the same upserted row, matching the prompt's "comment is a follow-up update" wording exactly. `conversationId`/`turnIndex`/`clientId` are threaded down from `App.tsx` (`activeId`) through `TurnView`→`AssistantResponseCard`→`MessageActions`; all three are `undefined` for every drill-panel turn (`DrillChat.tsx`'s own `<TurnView>` call never passes `conversationId` at all, confirmed by reading its render code directly rather than assuming), so feedback there is a local-only, never-persisted no-op by construction — no thread_id plumbing or special-casing needed, exactly the "may skip persistence" the prompt allows. New `lib/feedbackCopy.ts` (bilingual EN/HE, same `Record<Lang,{...}>` shape as `loginCopy.ts`) replaces and removes the now-dead placeholder `ANSWER_FEEDBACK` dict from `placeholderCopy.ts`. No feedback UI on error responses (the error branch in `TurnView.tsx` has its own retry-only markup, never renders `MessageActions` at all — true before this task, unchanged). 19 new backend tests (store upsert/resubmit/clear/scoping, route create/update/clear/404, transcript hydration attaches to the RIGHT turn across multiple turns, HTTP-level 422s for a bad rating enum value / >500-char comment / negative turn_index) + 2 new frontend tests (`turnsFromDetail` feedback hydration); full suite green (687 passed, pre-existing flaky test did not reproduce); `npm run lint`/`build`/`test` (111 vitest) all green. Verified live end-to-end against the real demo Postgres/conversation store (not mocked): asked a real question, thumbs-up (confirmed `POST /api/feedback` 200), switched to thumbs-down (confirmed the upsert replaced the row rather than adding a second one), typed a Hebrew comment ("המספרים לא נכונים") and sent it as a follow-up update, confirmed via direct API inspection that the ONE persisted row had `rating: "down"` and the correct comment; reloaded into a fresh chat, reopened the conversation from the sidebar (a REAL server fetch through `openConversation`/`turnsFromDetail`, not a localStorage restore) and confirmed exactly the right turn's thumbs-down rendered as `aria-pressed="true"` with every other turn correctly unpressed; cleared the rating and confirmed via the API that the row was deleted, not just nulled out |
| `alert_templates_prompt.md` | **Design decision (not literally "generate raw SQL text per template"):** one FIXED, single-SELECT, parameterized SQL query per KPI_CATALOG metric (revenue/orders/aov/churn_risk — the same 4 home_config already exposes, no open metric picker) fetches a plain daily (date, numerator, denominator) series from the read-only role; ALL 4 templates (pct_change/absolute_threshold/anomaly/streak) are then pandas post-processing over that series, not per-template SQL generation — this eliminates the injection surface entirely (threshold_pct/value/n_sigma/n_days never touch SQL text; only two server-computed anchor dates are ever bound as params) while still satisfying "reuse the existing resolution, never re-parse metric SQL" and the single-SELECT/no-system-catalog safety bar (verified: every metric query passes `agent/safety.is_safe()` once its placeholders are filled with literals). New `sema_core/alert_templates.py`: `_METRIC_SQL` (per-metric `kind`: flow=additive/revenue&orders, ratio=true-weighted-average-not-average-of-averages/aov, stock=snapshot-take-last-not-sum/churn_risk — found and fixed a real bug here: `Series.replace(0, pd.NA)` on an int64 column silently produces `pd.NA` objects that crash `.astype(float)`, switched to `.mask(col == 0)`), `validate_params()` (type/range checks — the one place an injection attempt is rejected, before any condition string or SQL exists), `evaluate_current()` (today's single reading) and `dry_run()` (90-day historical fire count + dates, required before create/enable). New `sema_core/org_alerts_store.py` (`org_alerts` + `org_alerts_migrations` SQLite tables, 12-enabled-per-client limit, UNIQUE(client_id, name)) + `sema_core/org_alerts.py` (migration: **3 of the 6 existing YAML alerts are cleanly expressible and migrated** — `revenue_mom_drop`→pct_change/mom/drop/8%/critical, `aov_drop`→pct_change/mom/drop/5%/warning, `churn_risk_count`→absolute_threshold/above/300/warning — seeded once per client, idempotent even if a migrated row is later deleted; **2 stay YAML/system, per the prompt's own fallback**: `active_customers_drop` (its metric isn't in the fixed KPI_CATALOG at all) and `churn_risk_high_value` (its SQL is a VIP-filtered subset, not the churn_risk metric's own value) — both audited as still-editable-only-via-enable/threshold in the pre-existing Home-config accordion, unchanged; deprecated-metric auto-disable persists a reason and clears it (without auto-re-enabling) if the metric comes back, mirroring `home_config._metric_is_usable`). `sema_core/alerts_engine.py`: `catalog()`/`_raw_alert_readings()` skip the 3 migrated YAML ids (no double-listing); `evaluate_all_alerts()` gained an `org_alert_readings` param merging org-alert breach readings into the EXACT SAME `_triggered_from_readings`/`filter_for_scope` pipeline YAML alerts already use — zero changes to that pipeline itself. `sema_core/db.py`'s `run_sql_readonly()` gained an optional `params` dict (mirroring `run_query`), backward-compatible (all existing keyword call sites unaffected). `api/main.py`: 7 new endpoints under `/api/admin/alerts` (templates catalog, metrics catalog, unified org+system list, dry-run test, create/PATCH/DELETE) — create and any metric/template/params-changing PATCH re-run the dry-run test server-side (not a client-supplied token, so no stale-token replay risk) and 422 if it fails; audited as `alert.created`/`updated`/`toggled`/`deleted` (toggled = enabled-only change, updated = anything else) via the existing `_audit`/`_diff_fields` helpers. Frontend: `AlertBuilder.tsx` (new) replaces the `product_placeholders_prompt.md` popover — real create/edit/delete card list + a modal builder (metric select → 4 bilingual template radio cards fed from the server catalog → dynamic param fields per type → Run test showing green/amber/red inline → Save disabled until a test passed WITH the current param values, any param edit invalidates it); `lib/alertCopy.ts` (bilingual EN/HE form copy, same `Record<Lang,{...}>` pattern); `lib/api.ts` gained the matching types/methods. The pre-existing `AlertsAccordion` (toggle+threshold cards) is UNTOUCHED and now automatically shows only the 2 remaining system alerts. Added `alert.*` bilingual sentence templates + an "Alerts" audit category (both frontend-only, additive) so the audit log reads naturally instead of falling back to the generic "{actor} performed {action} on {target}". 63 new backend tests (28 template-engine — SQL safety, injection rejection via type validation, flow/ratio/stock bucketing correctness including the zero-orders-is-NaN-not-a-crash case, all 4 templates against hand-computed synthetic series, 90-day dry-run windowing; 15 migration/store — exact threshold preservation, idempotent re-migration after manual deletion, deprecated-metric auto-disable + reason-clearing, the 12-limit at both create and enable-toggle time; 20 API-layer — full CRUD, audit events, 409 on duplicate name/over-limit, 422 on bad params/unsupported metric/injection attempt); full suite green (668 passed, the one pre-existing flaky timestamp-race test in `org_settings_store` did NOT reproduce this run); `npm run lint`/`build`/`test` (109 vitest) all green. Verified live end-to-end against the real demo Postgres (not mocked) for every template: `pct_change` (mom/wow/dod), `absolute_threshold`, `anomaly` (28-day baseline z-score), and `streak` all returned correct real fire-counts/dates via curl before any UI existed; then in the actual browser (Hebrew chrome): opened the Home-config Alerts accordion and saw the 3 migrated cards with correct Hebrew summary sentences, created "Orders Below 10" (absolute_threshold) end-to-end through the real modal, confirmed Save was disabled until Run Test passed and re-disabled the instant a param changed, edited its value 10→20 and confirmed the card and server both updated, deleted it via the confirm popover, and confirmed all 3 actions appear in the Audit log with the new natural-language sentences and a filterable "Alerts" category |
| `data_sources_add_prompt.md` | MySQL/MSSQL note per the prompt's own instruction: **both ended up AVAILABLE, not driver_pending** — `pymysql` is pure-Python (zero native deps); `pymssql` ships a manylinux wheel with FreeTDS statically bundled, confirmed installing cleanly with zero system packages inside the real Linux API container (verified with a real, correctly-erroring connection attempt proving the native bindings load). New: `sema_core/secrets.py` (Fernet encrypt/decrypt/fingerprint via `SEMA_SECRETS_KEY`, non-reversible SHA256 fingerprint, key read fresh per-call not cached); `sema_core/client_connections_store.py` (never returns plaintext/encrypted password except an internal-only decrypt method used solely by the connection-test probe); `sema_core/connector_catalog.py` (10 connectors, 4 groups, server-driven so the frontend never hardcodes availability); `sema_core/db_probe.py` (`test_connection()` — real connect + table list + a read-only write-permission probe per engine via `has_schema_privilege`/`information_schema`/`has_perms_by_name`, never attempts a real write; `_scrub()` guarantees every failure returns a generic message, verified by monkeypatching the low-level `_connect()` to raise with realistic embedded host/user/password text and asserting it never reaches the response/logs); `sema_core/source_requests_store.py` (Priority/Salesforce/Sheets tracked requests, explicit `VALID_STATUSES`); `sema_core/uploads_store.py` + `sema_core/file_upload.py` (CSV/xlsx via pandas+openpyxl, type inference + identifier sanitization + collision handling, table creation via `psycopg2.sql.Identifier`/`sql.SQL` composable objects — never string interpolation — for CREATE SCHEMA/TABLE/INSERT); `sema_core/db.py` gained `run_write`/`run_write_many` (full-access pool, commit/rollback matching existing pooled-query conventions). `api/main.py`: 9 new endpoints (catalog, test, connections POST/PATCH, requests, interest, sheets-info, upload, uploads/{id}/replace) plus a rewritten `admin_list_data_sources` merging 4 origins (config/connection/request/upload) into one `DataSource` card model with a `_reject_credential_like_fields()` blocklist (password/secret/token/api_key/credential/private_key/access_key/etc.) guarding the no-secrets-in-SaaS-requests rule. Frontend: rewrote `AddDataSourceModal.tsx` to fetch the server-driven catalog and route to 4 new flows — `SqlDbWizard.tsx` (4-step: details → test incl. write-access warning + ack checkbox + ready-to-run read-only-role SQL snippet → schema review/primary-table-and-date-field → done), `SaasRequestForm.tsx` (non-secret fields only), `SheetsFlow.tsx` (SA-email instructions), `CsvUploadFlow.tsx` (server-side parse, no client-side CSV dependency, shows resulting column plan as confirmation); `DataSourcesScreen.tsx` gained `RequestProgressRail` (4-step visual rail) and per-origin card rendering (fingerprint chip, Replace-file action for uploads). 21 new backend tests covering encryption round-trip, fingerprint non-reversibility, catalog gating, test-before-store and write-ack enforcement, credential absence from every response/list/audit/log surface, error scrubbing, SaaS credential-field rejection, CSV type inference/sanitization/collision handling and upload-creates-table (via a `fake_writes` fixture monkeypatching `file_upload.db.run_write*` — keeps the "no live DB in tests" invariant even for a feature that fundamentally writes to Postgres), unsupported-extension rejection, replace-file same-table-name. Full suite: 604 passed, 1 pre-existing unrelated flaky test (`test_org_settings.py::test_update_flags_conflict_when_stale_but_still_applies`, a timestamp-resolution race). Docker image rebuilt with the 4 new deps (`cryptography`, `pymysql`, `pymssql`, `openpyxl`); lint/vitest/build all green (one build-only `tsc -b` type error caught and fixed: `uploadFile()`'s generic wasn't inferred at the call site, needed explicit `uploadFile<UploadInfo>(...)`). Verified live end-to-end against the real demo Postgres and the real API container for all 4 paths: (1) SQL wizard — connected as the full-access `sema_user` deliberately to also exercise the write-warning path, saw the exact correct read-only-role SQL snippet, picked `orders`/`order_date` as primary table/date field, resulting "Warehouse Postgres" card shows Healthy with a fingerprint chip and real data-age query; (2) SaaS request — requested Priority ERP, resulting card shows the "Syncing" progress rail (Requested→Configuring→Testing→Active); (3) CSV upload — uploaded a real 3-row/4-column file, server correctly inferred TEXT/DOUBLE PRECISION/BOOLEAN types, resulting card shows a working Replace-file action; (4) coming-soon connector (Shopify) — confirmed `POST .../interest` returns 200 and no flow/dialog opens, gallery stays open. Demo data from this live pass (one connection, one request, one CSV-backed table) was intentionally left in place — there is no delete endpoint (correctly out of scope for this prompt) and the records are inert, additive proof the feature works rather than something needing cleanup |
| `product_placeholders_prompt.md` | Frontend-only, zero backend changes, existing tests untouched (109 vitest + 584 pytest still green). New shared infra: `lib/useUiLang.ts` (reactive `document.documentElement.lang` reader via MutationObserver — lets any component pick bilingual copy off the Part-2 `<html lang>` attribute without threading a language prop through the whole tree) + `lib/placeholderCopy.ts` (bilingual EN/HE dict for all 5 areas, same `Record<Lang,{...}>` shape as `loginCopy.ts`). **1. Sidebar user row:** new `UserFooter.tsx` — real identity via the existing `/api/admin/me` (not hardcoded), initials avatar, bilingual role label, menu (Personal settings disabled+"coming soon" / Log out → `/login`); renders nothing if the mock identity doesn't resolve for this client (graceful, same rule the rest of the admin panel already follows). **2. Gear two-tier menu:** `WorkspaceSwitcher.tsx`'s gear now opens a small menu (Manage organization → same `onOpenAdmin` as before / Platform console disabled+"coming soon") instead of navigating straight to admin. **3. Data sources gallery:** new `AddDataSourceModal.tsx` — the REAL gallery component (`data_sources_add_prompt.md` keeps it, only replaces the click handlers): 10 connectors across 4 groups (SQL DBs, business systems, easy sources, coming soon), every card's click shows a toast via the existing admin `ToastProvider`; wired into `DataSourcesScreen.tsx`'s header next to Refresh. **4. Alerts "New alert":** a small popover in `HomeConfigScreen.tsx`'s Alerts accordion previewing the 4 future template types (percent change / absolute threshold / anomaly / streak), each disabled with a "coming soon" tag. **5. Answer thumbs:** `MessageActions.tsx` gained up/down icons next to Copy/Retry, local flash feedback ("Feedback will be saved soon") matching `CopyButton`'s own existing transient-state convention — no new toast infra needed for the main chat shell, which has none today. Lint/vitest/build all green; verified every area live in the browser with the org's real chrome language set to Hebrew (confirmed via `document.documentElement.lang === "he"` in this environment): sidebar row shows "Miri Levi · מנהל/ת", its menu shows "הגדרות אישיות"/"התנתקות", the gear menu shows "ניהול הארגון"/"קונסולת פלטפורמה", the full connector gallery and its "בפיתוח" toast, the 4-template alert popover, and the thumbs' "הפידבק יישמר בקרוב" flash — all render correctly bilingually and RTL, and "Manage organization" still opens the real admin panel unchanged |
| `org_settings_gapfix_prompt.md` | **Part 1 (retention actually runs):** new `sema_core/retention_store.py` (`RetentionRunStore`, one row per client, 20h idempotency guard); `sema_core.retention.run_all_clients_sweep_tracked()` wraps the existing `run_sweep_for_client` (never reimplemented) with the skip guard, a persisted run record, and a system-actor audit event (`org.retention_swept`, `SYSTEM_ACTOR = {"id": None, ...}`) on non-zero deletions only; `api/main.py` gained a `lifespan` context manager (this app had none before) starting a `_retention_loop()` background asyncio task — ~60s after boot, then every 24h, each iteration wrapped in try/except, disableable via `SEMA_RETENTION_ENABLED` (default on); the actual sweep runs via `asyncio.to_thread` so it never blocks concurrent chat requests. `OrgSettings` gained 4 `last_retention_*` fields (embedded in the existing GET, no new endpoint) and `OrgSettingsScreen.tsx` shows the real "Last cleanup: N ago · M deleted" line (off/error states too). **Part 2 (org language = chrome only):** `PublicOrgSettings` gained `language`; `App.tsx` sets `document.documentElement.lang` (browser default at boot via the reused `lib/loginCopy.detectBrowserLang`, org setting once resolved) — explicitly NOT `dir`, per a correction the user made directly to `AGENTS.md` mid-task ("Layout is anchored — sidebar ALWAYS on the left... do not set dir='rtl' on the app shell/body") that I caught and fixed immediately (my first pass had wrongly flipped the whole shell's dir). Answer language untouched — already correctly mirrors the question per-turn via existing `dirOf(question)` plumbing, verified this still holds independent of chrome language. Added the explicit split as a new AGENTS.md bullet under Agent Voice. **Part 3 (audit semantic drafts):** `semantic.draft_saved`/`draft_discarded`/`draft_archived` now audited at the existing PUT/DELETE draft routes via a new `_diff_fields()` helper (before/after limited to genuinely changed keys; a brand-new draft with nothing to diff against records the full payload, not an empty diff) + matching bilingual sentences in `auditSentences.ts`. 20 new backend tests (5 retention-tracking scenarios incl. the 20h skip guard and cross-client failure isolation, 2 public-settings-language, 1 three-events-exactly-once for Part 3, plus a new `_isolated_retention_run_store` conftest fixture); full suite green (584 passed, 1 known pre-existing flaky timestamp-race test in `org_settings_store` unrelated); lint/build clean. Verified live end-to-end: the REAL background loop had already run against both real demo tenants before I even opened the browser (confirmed via `retention_runs` rows in the live `var/sema_state.db`); flipped ecommerce to a 30-day policy in the live UI and watched the status line update to the real run data, confirmed `org_settings.updated` in the live audit log, then reverted to "forever" to leave the demo clean; confirmed `document.documentElement.dir` stayed `"ltr"` after the language-chrome change while a mixed-language sidebar item (a Hebrew conversation title) rendered correctly alongside English ones, unaffected |
| `auth_login_prompt.md` — Part 0 only | Full login page UI, frontend-only, zero backend/session/cookie code (Parts A/B untouched, still blocked on hosting). `lib/loginCopy.ts` (bilingual EN/HE dictionary for every §2.1/§2.2 string, `detectBrowserLang`/`dirFor`); `hooks/useLoginFlow.ts` (pure `loginReducer` + a thin hook wrapping it with mocked ~700ms async delays) drives email → code → verify → redirect, with a `DevPreset` union covering all 7 reviewable states (code sent, wrong code, expired code, uninvited, suspended, expired invite, network error); `components/login/{LoginPage,EmailStep,CodeStep,ErrorScreen,SSOButton,DevStatePanel}.tsx`. SSO buttons are hand-coded official-brand SVGs (Google 4-color G, Microsoft 4-square), disabled with a "Coming soon" tooltip — no new icon-library dependency. `/login` wired in `main.tsx` as a plain `window.location.pathname` check (same no-router approach as the admin panel) so it renders OUTSIDE the app shell entirely; the mocked "successful" verify redirects to `/` after a beat since the app itself stays reachable with the mock identity, unchanged. Invite-link simulation via `?client_id=`, reusing the existing public `/api/org-settings` endpoint (no new backend surface) to show the org's real name/logo above the buttons. 21 new Vitest unit tests (reducer transitions + copy-dictionary completeness in both languages) — the codebase has no component-rendering test infra (no `@testing-library/react`/jsdom) so per the "no new deps without asking" convention this wasn't added; logic was made unit-testable by extracting it into pure `.ts` modules instead, matching the existing test suite's own pattern. `npm run test` (109 passed), lint, and build all green; verified live end-to-end in the browser: all 7 dev-panel states, the real email→code→verify→redirect arc back into the mock-identity app, the invite-context "Joining E-Commerce" banner, and grepped the new files for any non-logical (hardcoded left/right) Tailwind classes that would break RTL — none found, consistent with the rest of the app's RTL-safe conventions |
| `client_panel_screens_prompt.md` — Phase 4 (Data sources) | `config/clients.yaml` gained an optional `data_sources` block per client (postgres_main for both demo tenants; schema-ready `priority`/`salesforce` catalog entries, no connector implemented); new `sema_core/data_sources.py` — entities/dependent-metrics are DERIVED (not hand-maintained) by walking every metric's main SQL (via the existing `tables_in_sql`) PLUS each dimension's `requires_join` PLUS each alert's SQL, so e.g. insurance's policyholders/agents/vehicles/drivers surface correctly even though they only ever appear via a dimension join, never a metric's base query; `source_health()` does a real reachability check + a real `MAX(date)` age query (never fabricated), cached nowhere but cheap after pool warmup (~ms); `GET /api/admin/data-sources` (status cards + trust-chain entities) and `POST .../report-problem` (creates a real audit event via `_audit`, which now RETURNS the persisted row — `log_admin_event`/`audit_store.record`'s return value was previously discarded — + logs a platform notification, no email); `SchemaTable` gained a `source` field (postgres/priority/salesforce, defaults to "postgres") resolved via a new `source_type_for_entity`, distinct from the source *instance id* used internally — surfaced as a badge in the existing `EntitiesTab.tsx`. Evidence-panel integration: `_clean_evidence` appends a deterministic caveat (`stale_source_caveat`, same pattern as `_knowledge_caveats`) ONLY when a queried entity's source is unreachable — data age alone is never treated as a failure, so the 30-day-old demo data (generator's END_DATE vs. today) never falsely flags. Found & fixed a test-suite risk while wiring this in: without isolation, every evidence-panel test that reference a real table would open a live Postgres connection on every run (~2s cold, breaking the documented "no live DB in tests" guarantee) — added an autouse `_isolated_data_source_health` fixture in `tests/conftest.py` (default: reachable) so the whole suite stays deterministic; `tests/test_data_sources.py`'s own tests override it per-test to exercise the error path. 20 new backend tests, full suite green (577 passed), lint/build clean; verified live in the browser: status card (Healthy, 6 entities, 30-day data age), trust-chain expander (`orders → active_customers, aov, ... ← PostgreSQL`), report-a-problem confirm flow → audit log shows it under a new "Data sources" category, Entities tab badge shows "PostgreSQL". Noted but NOT a regression: the admin panel's mocked identity isn't workspace-aware, so switching to Auto Insurance in the sidebar still shows the ecommerce tenant's admin data — a pre-existing limitation of `current_identity()` (same for every other admin screen), confirmed unrelated by testing the insurance entity/metric mapping directly at the module level instead |
| `semantic_seed_prompt.md` | `sql/semantic/{rules,knowledge,glossary}.yaml` (5/5/12) + `sql/insurance/semantic/{rules,knowledge,glossary}.yaml` (3/3/8); `status`+`synonyms` on all 19 metrics; `domain` added to the 9 insurance metrics (was missing, blocked loading); baseline versions recorded (ecommerce v8, insurance v1); fixed `test_reserved_files_are_not_treated_as_metrics` which had assumed prod had no reserved files; verified live against the real model (rule citation, checkout-outage caveat, Hebrew glossary resolution) and in the admin Semantic model screen |
| `off_topic_and_recommendations_prompt.md` — §8a only | Copy control on every recommended-action card: `RecommendedActions.tsx` card changed from `<button>` to a `role="button"` div so it can nest a real `<button>` copy control (same pattern as `KpiCards.tsx`), reuses `CopyButton`/`copyText`; new `stripMarkdown()` in `lib/clipboard.ts`; bilingual `aria-label` via `COPY_LABEL[dir]`; single shared component covers main answer, off-topic (`ModeCard.tsx`), and DrillChat (`TurnView`→`AssistantResponseCard`) already — no DrillChat-specific change needed. Lint + build pass; verified live (renders, doesn't trigger the card's own drill-open; clipboard write itself is blocked by the automated browser sandbox for ALL copy buttons in the app, pre-existing and unrelated to this change) |
| `client_panel_screens_prompt.md` — Phase 2 (Audit log) | `sema_core/audit_store.py` (new, append-only `audit_events` table with DB-level triggers blocking UPDATE/DELETE, indexed `(client_id, created_at)`/`(client_id, actor_id)`); `sema_core.obs.log_admin_event()` — the one call site wired into all 8 existing admin mutations (invite/role/status/data_scope/remove/resend, semantic publish/restore, org-settings + logo updates), including 3 (role/status/data_scope PATCH, delete) that had NO audit logging at all before; `GET /api/admin/audit` + `/export` (paginated, filtered, CSV capped 10K); frontend `AuditLogScreen`/`AuditEventDrawer`/`useAuditLog`/`auditSentences.ts` (bilingual sentence templates, category tags, impersonation amber-bar styling ready for when that feature lands); nav flipped active. 16 new backend tests + `pytest`/lint/build green; verified live end-to-end in the browser (filters, drawer diff, CSV download headers confirmed via curl since fetch() can't see cross-origin Content-Disposition). Found & flagged separately (task_02cdd538, not fixed here): a pre-existing flaky test in `org_settings_store` unrelated to this work |
| `client_panel_screens_prompt.md` — Phase 3 (Home screen) | Scope note (asked the user up front, no answer given, took the Recommended/constrained option): the KPI/pulse/alert catalogs are the app's existing computed things, not a generic "run any certified metric as a KPI" engine — that would need a new deterministic SQL-re-scoping subsystem, out of scope. `sema_core/home_config.py` (new: fixed 4-KPI catalog revenue/orders/aov/churn_risk, defaults, publish-time `validate()`, render-time `effective_config()` that drops deprecated-metric KPIs with a warning — deliberately NOT gated on "certified" status like validate() is, since churn_risk is intentionally 'draft' in the seeded demo content and is the app's long-standing default KPI); `sema_core/home_config_store.py` (new, one JSON blob per client, draft/publish/revert); `overview.py`/`daily_brief.py`/`alerts_engine.py` all gained an optional `config` param (backward-compatible, `None` reproduces exact prior behavior) — `alerts_engine` refactored into a cached config-independent raw-reading layer + a stateless config-aware breach decision so a threshold override never needs to bust the cache; 5 endpoints (`GET/PUT/publish/revert/preview` on `/api/admin/home-config`, preview is POST-with-body so it reflects the in-memory draft instead of racing the separate autosave debounce) + `/api/admin/alerts-catalog`; `/api/overview`, `/api/brief`, `/api/alerts`, `/api/popular-questions`, `/api/clients` all resolve the published config. Frontend: `HomeConfigScreen` (4 accordions, up/down-arrow reorder in place of mouse drag for better keyboard accessibility, live preview reusing the REAL `HomeDashboard` component read-only) + `useHomeConfig` (flush-before-publish / cancel-before-revert / resync-after-failure — two real race conditions found and fixed live in the browser: Publish could 409 on a just-edited-but-not-yet-autosaved change, and Revert could 409 the same way and leave `hasDraft` permanently stuck lying about server state). 28 new backend tests, full suite green (556 passed), lint/build clean; verified live end-to-end: edit → live preview updates → publish → real `/api/overview` and the real home dashboard (after reload) both reflect the new KPI set → revert restores the prior published config, confirmed under a deliberately raced edit-then-immediately-revert click |
| `admin_users_screen_prompt.md` incl. deltas | invited_by/joined_at in `org_user_store.py`, `GET /api/admin/roles`, `UserDetailDrawer.tsx`, `RoleTooltip.tsx` |
| `data_access_scope_prompt.md` | `sema_core/data_scope.py`, `/api/admin/data-scopes`, enforcement in `agent/tools.py` + `response.py` (`access_denied`), `DataScopeControl.tsx` |
| `off_topic_and_recommendations_prompt.md` (core) | witty off-topic in `prompts.py`, structured actions schema in `response.py`, `RecommendedActions.tsx` badges |
| `client_panel_screens_prompt.md` — Phase 1 | org-settings CRUD `api/main.py:1239+`, `OrgSettingsScreen.tsx`, nav active |
| `semantic_model_screen_prompt.md` | Full screen, 5 tabs, draft/validate/publish/restore, 22 backend tests |
| `drill_threads_prompt.md` | `DrillChat.tsx` thread resume, `ThreadBadge.tsx`, `useThreads.ts`, store persistence |
| `chat_ux_fixes_prompt.md` | `AnswerSummary` in `AssistantResponseCard.tsx`, `useChatScroll.ts` top-align on answer |
| `daily_brief_prompt.md` | Two-layer in `daily_brief.py`: pulse + 14d sparkline, 6 generators, MAX_INSIGHTS=3 |
| `progress_panel_prompt.md` | `ProgressPanel.tsx` + `lib/progress.ts`, retry as same-line state |
| `wide_screen_layout_prompt.md` | staged container `max-w-3xl xl:max-w-5xl 2xl:max-w-6xl` in `App.tsx` |
| `sidebar_redesign_prompt.md` | (from earlier session summary — implemented) |
| `remove_others_also_asked_prompt.md` | string absent from `frontend/src` |

Notes:
- Order of pending items is a suggestion; the human may reorder by editing this table.
- Every prompt file is self-contained; do not read other queue items' prompts unless referenced.
