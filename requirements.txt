# SEMA - Python dependencies
#
# Database + data generation (this step):
pandas==2.2.3          # data manipulation; builds the synthetic tables
pytz                    # already a transitive dep of pandas; used directly now for
                        # the org-settings IANA timezone list (sema_core.org_settings_store)
numpy==1.26.4           # random number generation (with a fixed seed for reproducibility)
faker==24.4.0            # realistic-looking fake names, emails, etc.
psycopg2-binary==2.9.9   # PostgreSQL driver, used for bulk-loading via COPY
python-dotenv==1.0.1     # loads connection settings from .env

# Phase 1 UI:
streamlit==1.40.0        # chat-style web UI (>=1.39 for st-key-* CSS classes)
plotly==5.24.1           # interactive charts

# Agent layer:
pyyaml==6.0.2            # loads the semantic layer (sql/semantic/*.yaml)
sqlglot==25.24.0         # parses/validates agent SQL (SELECT-only, auto-limit)
anthropic==0.109.1       # Claude API client (the agent's LLM)

# Data sources add-flow (data_sources_add_prompt.md) -- explicitly pre-approved
# in that prompt's own text:
cryptography             # Fernet encryption-at-rest for stored DB credentials
pymysql                  # pure-Python MySQL driver (no system libs), for the
                          # self-service SQL DB wizard's MySQL connector
pymssql                  # MS SQL Server driver -- ships a manylinux wheel with
                          # FreeTDS statically bundled, confirmed installs
                          # cleanly with no apt-get/system packages required
openpyxl                  # pandas' .xlsx engine, for the Excel-upload connector
                          # (not named in the prompt's pre-approved list, but a
                          # direct, minimal, pure-Python requirement of the
                          # explicitly-requested "Excel/CSV upload" feature)
