# SEMA REST API layer (FastAPI). Separate from requirements.txt so the
# Streamlit app's dependencies are untouched. Install into the same venv:
#   .venv\Scripts\python.exe -m pip install -r requirements-api.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9  # required by FastAPI for UploadFile/form-data (org-settings logo upload)
